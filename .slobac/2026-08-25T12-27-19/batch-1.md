## Findings

### test_coverage_collection.py:59 — `test_default_pytest_does_not_write_lcov`

- **Location:** `test_coverage_collection.py:59`, `test_default_pytest_does_not_write_lcov`
- **Smell:** `conditional-logic`
- **Rationale:** The test claims that a default pytest invocation does not write `coverage/lcov.info`, but the assertion on that path runs only under `if not existed`. When the file already exists, one path through the test omits the central oracle and can pass even if the subprocess rewrites the file. This matches the asymmetric assertion-gating signal in [conditional-logic](https://texarkanine.github.io/slobac/taxonomy/conditional-logic/).
- **Prescribed remediation:** Isolate the subprocess in a temporary project or move the watched coverage destination into `tmp_path`, establish an unconditional precondition that the file is absent, then assert unconditionally that it remains absent. If preserving a pre-existing repository artifact is necessary, snapshot its bytes and metadata before the run and assert they are unchanged afterward.
- **Why this isn't a false positive:** This is not a parameterized matrix with assertions in both arms or a runner-native skip; the only branch has no alternate and suppresses the claimed filesystem assertion.

### test_fold.py:48 — `test_nap_stem_inherits_left_child_seq_prefix`

- **Location:** `test_fold.py:48`, `test_nap_stem_inherits_left_child_seq_prefix`
- **Smell:** `implementation-coupled`
- **Rationale:** The expected persisted stem is built with the SUT's underscore-prefixed `m._seq_prefix`, so a refactor or defect shared by filename generation and this private helper can make the test fail or pass without changing the externally observable storage invariant. This is direct private-helper oracle construction under [implementation-coupled](https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/).
- **Prescribed remediation:** Derive the expected left-child sequence prefix independently from the public filename contract, or assert the public relationship between the created nap filename and `pa.name` without calling `_seq_prefix`. Keep the assertions on the emitted `.summ` and `.tree` files.
- **Why this isn't a false positive:** Although Python permits same-project access to underscore names, this use is not incidental fixture setup; the private helper directly computes the expected value for the behavior being verified.

### test_recall.py:115 — `test_recall_matches_nested_nap_caption`

- **Location:** `test_recall.py:115`, `test_recall_matches_nested_nap_caption`
- **Smell:** `implementation-coupled`
- **Rationale:** The test computes its expected recall line by calling private `m._projected_child` on the exact internal tree child and then formats that result. A change to the internal projection shape breaks the test even if `recall_text` continues to return the same valid public line, and a shared projection defect can contaminate both actual and expected values. This matches [implementation-coupled](https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/).
- **Prescribed remediation:** Assert the public recall contract from independently known fixture data: one line containing the nested caption and a usable public ID, then prove that ID zooms to the expected dated leaves. Avoid private projection helpers in expected-value construction.
- **Why this isn't a false positive:** The underscore access is not merely sanctioned setup for a difficult fixture; it is the oracle used for exact equality against the SUT's output.

### test_wake_expand.py:73 — `test_native_notes_fill_budget_without_split`

- **Location:** `test_wake_expand.py:73`, `test_native_notes_fill_budget_without_split`
- **Smell:** `over-specified-mock`
- **Rationale:** Replacing `m.loads_tree` with a function that raises makes the test require zero calls to a particular collaborator. The claimed public behavior—four native file lines at budget four—could remain correct after a harmless eager-validation refactor that invokes `loads_tree`, yet this test would fail. This is the `verify(never())`/private-branch shape described by [over-specified-mock](https://texarkanine.github.io/slobac/taxonomy/over-specified-mock/).
- **Prescribed remediation:** Remove the `loads_tree` monkeypatch and assert observable outcomes: the exact four projected records, unchanged payload names, and unchanged view cardinality. Retain an interaction assertion only if a documented performance contract explicitly forbids tree reads, in which case instrument filesystem reads at that boundary and state the budget.
- **Why this isn't a false positive:** No documented protocol or externally owned call-count contract is present; the forbidden call is an internal implementation choice while the output and store state already provide behavioral oracles.

### test_wake_expand.py:233 — `test_zoom_expanded_child_id`

- **Location:** `test_wake_expand.py:233`, `test_zoom_expanded_child_id`
- **Smell:** `vacuous-assertion`
- **Rationale:** After selecting an expanded child ID, the sole oracle on `zoom_text` is `assert out`. Any unrelated non-empty string would satisfy it, so the test does not prove the name's claim that the selected ID zooms to that child's children or text. This matches the truthiness-only signal in [vacuous-assertion](https://texarkanine.github.io/slobac/taxonomy/vacuous-assertion/).
- **Prescribed remediation:** Assert the expected child-specific content from the fixture—such as the known nested caption or dated leaves—and, if relevant, assert that sibling-only content is absent. Prefer exact parsed line membership over generic truthiness.
- **Why this isn't a false positive:** The truthiness check is not a language-narrowing precondition followed by a stronger oracle; it is the final and only assertion about the zoom result.

### test_zipper.py:183 — `test_rematerialize_nap_stem_uses_leftmost_seq_child_id_and_leaves`

- **Location:** `test_zipper.py:183`, `test_rematerialize_nap_stem_uses_leftmost_seq_child_id_and_leaves`
- **Smell:** `implementation-coupled`
- **Rationale:** The test reaches through several underscore-prefixed internals—`_unlink_node`, `_seq_prefix`, `_digests_of_tree`, and `_nap_stem`—and explicitly asserts the private `_nap_stem` result. The persisted-file contract can be checked without pinning those helper boundaries, so internal decomposition or renaming would break the test despite unchanged files. This matches [implementation-coupled](https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/).
- **Prescribed remediation:** Arrange rematerialization through public store operations, independently derive the expected filename components from the documented on-disk format, and assert only emitted `.tree`/`.summ` names and bytes. Remove the direct `_nap_stem` assertion.
- **Why this isn't a false positive:** This is not harmless same-module fixture access; the private methods are both exercised and used as the expected-value oracle for the behavior named by the test.

### test_zipper.py:204 — `test_rematerialize_does_not_clobber_existing_dest`

- **Location:** `test_zipper.py:204`, `test_rematerialize_does_not_clobber_existing_dest`
- **Smell:** `implementation-coupled`
- **Rationale:** The non-clobber behavior is strongly verified by unchanged destination bytes, but the final assertion additionally calls private `m._nap_stem(child)` and compares it to the node name. That internal helper assertion is unrelated to whether existing destinations were preserved and couples the test to private naming decomposition. This matches [implementation-coupled](https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/).
- **Prescribed remediation:** Delete the `_nap_stem` assertion from this test and retain the independent before/after byte comparisons. If stem derivation needs its own contract test, cover it through public rematerialization output with independently derived expectations.
- **Why this isn't a false positive:** The private call is not needed for setup or type narrowing; it is an extra direct assertion on an internal helper outside the test's public non-clobber claim.

### test_zipper.py:351 — `test_heal_odd_arity_finishes_under_iteration_cap`

- **Location:** `test_zipper.py:351`, `test_heal_odd_arity_finishes_under_iteration_cap`
- **Smell:** `over-specified-mock`
- **Rationale:** The wrapper counts every call to `m.list_view`, raises after an arbitrary 50 calls, and then asserts the same internal call count. A terminating implementation that changes traversal strategy or makes 51 inexpensive view reads would fail despite preserving the observable heal result. This pins collaborator invocation quantity as described by [over-specified-mock](https://texarkanine.github.io/slobac/taxonomy/over-specified-mock/).
- **Prescribed remediation:** Test termination with a process-level timeout or runner timeout, then assert the healed store's unique cover and reachability. If a performance ceiling is a real contract, document a time or operation budget at the public boundary instead of counting calls to one internal helper.
- **Why this isn't a false positive:** The count is not tied to a documented retry, transaction, rate-limit, or protocol requirement; it is an implementation-specific proxy for non-hanging behavior.

### test_zipper.py:380 — `test_heal_malformed_overlapping_nap_skipped`

- **Location:** `test_zipper.py:380`, `test_heal_malformed_overlapping_nap_skipped`
- **Smell:** `implementation-coupled`
- **Rationale:** The final leaf-integrity oracle calls private `m._digests_of_tree(inner)`. That helper is part of the same implementation whose persisted tree semantics are under test, so a shared digest traversal defect can make expected and actual interpretation agree incorrectly, and helper refactors break the test without changing public behavior. This matches [implementation-coupled](https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/).
- **Prescribed remediation:** Verify retained leaves through public `leaf_digests`/view behavior or through independent parsing of the documented tree schema, and retain the explicit file-existence checks. Do not use `_digests_of_tree` as the expected-value oracle.
- **Why this isn't a false positive:** The private helper does not merely create the malformed fixture; it determines the semantic contents asserted after healing.

### test_zipper.py:447 — `test_same_second_notes_keep_left_child_stem`

- **Location:** `test_zipper.py:447`, `test_same_second_notes_keep_left_child_stem`
- **Smell:** `implementation-coupled`
- **Rationale:** The expected left sequence is obtained from private `m._seq_prefix` and then compared with the rematerialized nap name. This duplicates the production naming path in the oracle, couples the test to an underscore helper, and can hide a common defect. It is the private-access signal in [implementation-coupled](https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/).
- **Prescribed remediation:** Independently extract the documented timestamp-random prefix from the known left child's filename or compare the emitted name against a public format pattern rooted in that filename. Avoid `_seq_prefix` in the expectation.
- **Why this isn't a false positive:** The private method directly supplies the expected prefix; it is not only an allowed Python test fixture convenience.

### test_zipper.py:483 — `test_cli_note_and_nap_call_heal`

- **Location:** `test_zipper.py:483`, `test_cli_note_and_nap_call_heal`
- **Smell:** `over-specified-mock`
- **Rationale:** The test wraps `heal_view` and requires exact counts after two `note` commands and one `nap`, then requires zero calls for `wake`. These counts lock CLI orchestration to one internal collaborator and call frequency rather than asserting the externally promised unique-cover and read-only outcomes. This is exact interaction pinning under [over-specified-mock](https://texarkanine.github.io/slobac/taxonomy/over-specified-mock/).
- **Prescribed remediation:** Seed overlapping stores, invoke each CLI command, and assert observable postconditions: mutating commands produce a unique cover while `wake` leaves payloads unchanged. Keep call counts only if `heal_view` invocation itself is a documented plugin/protocol contract.
- **Why this isn't a false positive:** The exact counts are not a retry, transactional, or externally owned call-count requirement; they expose the current internal dispatch design.

### test_zipper.py:560 — `test_cli_invalid_nap_caption_does_not_heal`

- **Location:** `test_zipper.py:560`, `test_cli_invalid_nap_caption_does_not_heal`
- **Smell:** `over-specified-mock`
- **Rationale:** The test already proves the public failure contract with a nonzero exit and unchanged payload names, but also wraps `heal_view` and requires zero calls. A refactor that safely inspects or normalizes the store before rejecting the caption could preserve all external behavior and still break this assertion. This is unnecessary `verify(never())` interaction pinning under [over-specified-mock](https://texarkanine.github.io/slobac/taxonomy/over-specified-mock/).
- **Prescribed remediation:** Remove the `heal_view` spy and keep the exit-status plus complete before/after payload comparison. If “validation occurs before any store mutation” is the contract, assert filesystem state and bytes, not which internal function was skipped.
- **Why this isn't a false positive:** The interaction is incidental to the already asserted no-write result and is not a documented ordering protocol.

## Behavior Summaries

| File | Line | Test ID | Behavior | Tier | Smells Found |
|------|------|---------|----------|------|--------------|
| test_coverage_collection.py | 34 | test_cov_summem_emits_lcov_with_summem_sf | Runs a nested pytest invocation against the version test with `--cov=summem` and an LCOV destination in `tmp_path`, using a cleaned coverage environment. It asserts a zero subprocess status, an emitted LCOV file, at least one `SF:` record naming the no-suffix `summem` script, and a source set that is not tests-only. Fixtures comprise the repository root, a temporary coverage data file, and parsed LCOV source paths. | integration | — |
| test_coverage_collection.py | 59 | test_default_pytest_does_not_write_lcov | Runs the same narrow nested pytest target without coverage flags under a cleaned environment. It asserts the subprocess succeeds and no LCOV file appears in `tmp_path`; when the repository-level `coverage/lcov.info` did not pre-exist, it also asserts that path remains absent. The fixture records only the watched path's initial existence, which leaves the central repository-path oracle conditional when an artifact already exists. | integration | conditional-logic |
| test_fold.py | 48 | test_nap_stem_inherits_left_child_seq_prefix | Creates two timestamped notes, computes their leaf-set ID, and calls `write_nap` on the two public node IDs. It verifies that both `.summ` and `.tree` files exist under a stem composed from the left note's sequence prefix, the computed leaf-set, and leaf count two. The fixture is a temporary initialized store with deterministic timestamps and RNG seeds; the expected prefix is obtained through private `_seq_prefix`. | integration | implementation-coupled |
| test_fold.py | 63 | test_same_second_nap_stays_in_left_slot | Writes four notes in one UTC second with a shared deterministic RNG, naps the two oldest view nodes, and rereads `list_view`. It asserts the resulting ordered leaf grains are exactly `[2, 1, 1]`, proving the new two-leaf nap inherits the left slot instead of sorting behind either remaining note. The fixture is an isolated temporary store whose same-second names differ only through generated random suffixes. | integration | — |
| test_fold.py | 76 | test_equal_grain_pair_returns_two_oldest_ids_when_all_ones | Builds three one-leaf notes with increasing timestamps, obtains their ordered view nodes, and passes them to the file-local `_equal_grain_pair` selection helper. It asserts the helper returns exactly the first and second node IDs, describing the intended oldest-adjacent choice when every grain is one. The fixture uses real `write_note` and `list_view` operations in a temporary store, while the selection routine exercised is test-local. | integration | — |
| test_fold.py | 86 | test_equal_grain_pair_returns_none_for_16_plus_1 | Writes sixteen deterministic notes, folds all loose IDs into one sixteen-leaf pack, then appends a later one-leaf note. It calls the file-local `_equal_grain_pair` over the real view and asserts the result is `None`, because adjacent grains sixteen and one are unequal. The fixture is an isolated persisted store; `fold_ids` constructs the large pack through production nap writes before the test-local selection check. | integration | — |
| test_fold.py | 96 | test_equal_grain_pair_returns_two_8s_not_16_plus_8 | Constructs one sixteen-leaf pack followed by two independently folded eight-leaf packs, all with deterministic timestamps and captions. It first asserts the persisted view grain sequence is `[16, 8, 8]`, then asserts the file-local pair selector chooses the IDs of the two adjacent eights rather than the sixteen-plus-eight boundary. The fixture exercises real note, nap, and view persistence around a test-local selection algorithm. | integration | — |
| test_fold.py | 111 | test_equal_grain_pair_returns_two_1s_not_2_plus_1 | Writes four timestamp-ordered notes and naps the oldest two, producing a real view with grains `[2, 1, 1]`. It asserts that view shape and then verifies the file-local `_equal_grain_pair` returns the last two IDs, not the unequal two-plus-one boundary. The fixture is a temporary store with deterministic names and a persisted two-leaf nap, so the selector receives production node objects in public order. | integration | — |
| test_fold.py | 124 | test_equal_grain_pair_duplicate_ids_when_same_text | Writes two notes with identical text at different timestamps, reads the view, and establishes that content-derived node IDs are equal. It then passes those nodes to the file-local `_equal_grain_pair` and asserts the returned tuple contains that same ID twice, preserving positional multiplicity despite ID equality. The fixture uses a temporary store and deterministic note names; assertions target duplicate-ID semantics and pair output. | integration | — |
| test_fold.py | 135 | test_over_budget_note_prints_saved_when_16_plus_1 | Creates a sixteen-leaf pack, changes into the temporary repository, sets `WAKE_LINES` to one, and invokes CLI `main(["note", "later"])`. It asserts success and exact stdout `Saved.\n`, plus absence of fold-request markers, demonstrating that an over-budget but unequal-grain view cannot propose a fold. The fixture uses captured stdout, a real persisted pack, deterministic notes, and a monkeypatched budget. | integration | — |
| test_fold.py | 150 | test_under_budget_note_prints_saved | Changes into an empty initialized temporary repository and invokes CLI note creation with default budget. It asserts exit status zero, exact stdout `Saved.\n`, and absence of both the run-command and compression prompt fragments. The fixture is deliberately minimal—one note, captured stdout, and no nap files—so the assertions isolate the under-budget CLI response rather than fold-selection behavior. | integration | — |
| test_fold.py | 162 | test_long_stream_same_second_grains_are_powers_of_two | Streams twenty-four same-second notes into a store with budget eight and repeatedly folds the oldest adjacent equal-grain pair while the view exceeds budget. It asserts the final view has at most eight nodes, every grain is a positive power of two, at least one grain reaches four, and no anomalous seventeen-leaf pack appears. The fixture combines deterministic same-second naming, real nap writes, and the file-local selector as a folding driver. | integration | — |
| test_fold.py | 184 | test_sixteen_leaf_pack_tree_depth_is_log | Writes sixteen notes, repeatedly folds adjacent equal-grain pairs until no pair remains, then reads the sole resulting nap tree. It asserts one view node remains, its leaf count is sixteen, and the deepest `NoteChild` is at depth at most four, establishing balanced logarithmic nesting. The fixture is a real temporary store; a recursive test helper computes depth from the deserialized production tree. | integration | — |
| test_fold.py | 201 | test_nap_prints_remaining_ones_not_parent_plus_one | Writes four ordered notes at budget two, invokes CLI `nap` on the oldest pair, and captures stdout. It asserts success, absence of `Saved.`, presence of dated lines for only the remaining `c` and `d` notes, and presence of the next nap command and “Invent nothing.” guidance. The fixture is a temporary current working directory with deterministic note stamps and public node IDs. | integration | — |
| test_fold.py | 219 | test_nap_prints_nothing_when_at_or_under_budget | Writes exactly two notes into a temporary store configured with budget two, invokes CLI `nap` on their IDs, and captures stdout. It asserts a successful exit and an exactly empty output stream, proving a completed nap does not emit either a save acknowledgement or another fold request when the resulting one-file view is at or under budget. Deterministic timestamps and monkeypatched configuration define the fixture. | integration | — |
| test_fold.py | 232 | test_over_budget_note_requests_equal_grain_ones | Seeds three notes at budget three, invokes CLI note creation for a fourth, and inspects the full fold prompt plus filesystem. It asserts save text precedes compression guidance, dated lines identify the two oldest notes, the command uses their short IDs without leaking full IDs, four notes remain, and no nap files were written. The fixture uses deterministic stamps, captured stdout, and direct payload enumeration. | integration | — |
| test_fold.py | 263 | test_default_wake_lines_is_32 | Loads the `summem` module and directly asserts that its exposed `WAKE_LINES` default equals 32. No filesystem store is created and no command runs; the assertion targets the module's default configuration constant as the complete contract. The fixture shape is only a fresh dynamically loaded module, making this a focused configuration-default check. | integration | — |
| test_fold.py | 269 | test_config_toml_wake_lines_is_read | Creates a store with committed `config.toml` setting `WAKE_LINES = 1`, writes one note, then invokes CLI note creation for a second. It asserts success, a fold request after `Saved.`, expected command guidance, exactly two note payloads, and no generated nap, proving store configuration controls the budget without auto-folding. The fixture combines a temporary current directory, captured stdout, and deterministic notes. | integration | — |
| test_fold.py | 288 | test_fold_request_mentions_remaining | Writes five notes under budget three and calls `fold_request` directly. It asserts the prompt reports exactly one compression remaining after the proposed one, displays the two oldest dated leaves, uses `.summem/summem nap`, and does not use the bare `summem nap` spelling. The fixture is a temporary selected store with deterministic timestamps and a monkeypatched module budget. | integration | — |
| test_fold.py | 304 | test_fold_request_identical_notes_use_short_prefix | Writes two identical notes at budget one and calls `fold_request`. It confirms their content-derived full ID is absent, computes the public short ID, asserts it is eight characters, and verifies the emitted nap command repeats that same short prefix for both positional operands. The fixture is an isolated current store with deterministic distinct filenames but duplicate text and therefore duplicate IDs. | integration | — |
| test_fold.py | 320 | test_fold_request_uses_config_entry_chars | Writes `ENTRY_CHARS = 140` into the store config, creates two notes over a one-line budget, and calls `fold_request`. It asserts the prompt contains `140 characters` and omits the default `280 characters`, proving the store-level entry-length setting is read into generated operator guidance. The fixture uses a temporary current repository, deterministic notes, and a monkeypatched wake budget. | integration | — |
| test_fold.py | 335 | test_fold_request_includes_path_when_cwd_misses_store | Creates a nested store that differs from the store selected by walking up from the current repository root, writes two notes there, and requests a fold at budget one. It asserts the generated nap command includes relative `--path foo/packages/baz`, proving commands remain targeted when `$PWD` would resolve elsewhere. The fixture contains nested directories, two deterministic notes, and a monkeypatched budget. | integration | — |
| test_fold.py | 350 | test_fold_request_omits_path_when_cwd_selects_store | Creates a store at `pkg`, changes into `pkg/src` so walk-up selects that same store, writes two over-budget notes, and calls `fold_request`. It asserts no `--path` option appears and the command begins with `.summem/summem nap`, proving redundant targeting is omitted when current-directory discovery is sufficient. The fixture is a nested temporary repository with deterministic notes and budget one. | integration | — |
| test_proof_branches.py | 14 | test_two_branch_packs_merge_then_nap_neighbors | Creates two Git branches from main, writes and folds four disjoint notes on each, commits both branches, and merges them into main. It asserts both merges succeed, wake shows two lines, napping the neighboring pack IDs produces one eight-leaf captioned line, zoom reaches an `A` and `B` leaf, and the parent file exists. The fixture is a real temporary Git repository with subprocess-backed branch operations and deterministic note times. | e2e | — |
| test_proof_branches.py | 60 | test_two_branch_overlapping_packs_heal_on_next_mutate | Commits common `A` and `B` notes, creates separate branches that fold overlapping `ABD` and `ABE` packs, merges both branches, then invokes CLI note mutation. It asserts both Git merges succeed, CLI note returns zero, the healed store has a unique leaf cover, and all four original branch sentences remain reachable. The fixture is a real divergent Git history with persisted overlapping trees and a temporary CLI working directory. | e2e | — |
| test_proof_reject.py | 34 | test_nap_help_exits_zero | Runs the actual no-suffix script in a subprocess with `nap --help`. It asserts return code zero and confirms stderr does not classify `nap` as an invalid choice, establishing that help recognizes the subcommand before any store is needed. The fixture uses the repository script path, the current process's Python interpreter, and captured byte streams; no temporary repository or payload is created. | e2e | — |
| test_proof_reject.py | 45 | test_nap_range_16_31_rejected_without_writing | Runs the real CLI subprocess in an initialized temporary repository with range token `16-31`, another ID, and a caption. It asserts nonzero status, an error naming the rejected token and instructing the user to copy an ID from wake, no generic invalid-choice text, and an empty payload-file set. The fixture observes both captured stderr and all non-dot files under notes and naps. | e2e | — |
| test_proof_reject.py | 57 | test_nap_hash_range_rejected_without_writing | Invokes the real `nap` command with hash-prefixed range `#2-5`, a second ID, and caption in a temporary repository. It asserts nonzero status, token-specific correction guidance, absence of argparse's generic invalid-choice wording, and no note or nap payload files. The fixture is an initialized Git repository plus captured subprocess streams and a helper that enumerates public store payloads. | e2e | — |
| test_proof_reject.py | 69 | test_nap_no_ids_rejected_without_writing | Runs the actual CLI `nap` command without operands in a fresh temporary repository. It asserts a nonzero return code, verifies stderr does not misreport the known subcommand as an invalid choice, and confirms no visible note or nap payload file was created. The fixture captures subprocess output and inspects the complete store payload surface after rejection. | e2e | — |
| test_proof_reject.py | 79 | test_nap_unknown_ids_rejected_without_writing | Runs the real CLI with two valid-looking but nonexistent short IDs and a caption. It asserts nonzero status, an `unknown id` diagnostic with wake-copy guidance, and an empty payload-file list, proving ID validation fails before writes. The fixture is an initialized temporary repository, captured subprocess stderr, and exhaustive enumeration of non-dot files in both payload directories. | e2e | — |
| test_recall.py | 14 | test_recall_matches_loose_note | Writes one loose note containing `hello`, calls `recall_text(repo, "hello")`, and asserts exact equality with the expected dated leaf line plus newline. The assertion simultaneously checks matching, timestamp projection, original text preservation, and single-line formatting. The fixture is a temporary initialized store with a deterministic timestamp and RNG seed and no naps or monkeypatched configuration. | integration | — |
| test_recall.py | 23 | test_recall_matches_caption | Writes two notes, folds them into a nap captioned `folded pair`, restricts wake to one line, and calls `recall_text` for `folded`. It asserts the returned text contains the exact caption, proving current-view nap captions are searched. The fixture is a temporary store with deterministic note order, one persisted nap, and a monkeypatched wake budget that keeps the pack represented as a caption. | integration | — |
| test_recall.py | 36 | test_recall_matches_sentence_inside_tree | Writes a uniquely named note and a sibling, folds them into one nap, and recalls the unique sentence after it exists only in the persisted tree. It asserts exact output equal to that leaf's dated line plus newline, proving nested original text remains searchable and projected with its original stamp. The fixture is a deterministic two-note temporary store collapsed to one captioned pack. | integration | — |
| test_recall.py | 48 | test_recall_output_omits_notes_naps_and_git | Writes a matching loose note, calls `recall_text`, and asserts that the returned user-facing output contains none of `notes/`, `naps/`, or `git`. The test targets abstraction-boundary hygiene rather than match correctness, using a simple matching fixture to ensure output is produced. The fixture is one deterministic note in an initialized temporary Git repository with no packs. | integration | — |
| test_recall.py | 59 | test_recall_matches_loose_note_outside_wake_window | Writes eleven ordered notes, lowers `WAKE_LINES` to four, and contrasts wake with recall for the oldest `n0`. It asserts `n0` is absent from `wake_text` but present in `recall_text(repo, "n0")`, proving recall scans the full view instead of only the wake window. The fixture is a temporary store with deterministic timestamps, names, and a monkeypatched display budget. | integration | — |
| test_recall.py | 70 | test_recall_skips_unreadable_sibling_warns | Builds two independent two-note packs, corrupts one pack's tree, clears capture buffers, and recalls a sentence from the good sibling. It asserts the good leaf is returned, stderr is exactly `skipped a pack\n`, and the warning leaks no store paths, Git wording, or traceback. The fixture is a temporary mixed-validity store with deterministic leaves and captured stdout/stderr. | integration | — |
| test_recall.py | 95 | test_recall_malformed_tree_does_not_raise | Creates one two-note pack, corrupts its sole `.tree`, and calls recall for a nested leaf. It asserts the call returns a string, emits exactly one `skipped a pack` warning, and exposes no notes path, naps path, Git term, or traceback. The fixture is an isolated malformed persisted pack with capture buffers cleared before the SUT call; assertions focus on graceful degradation and sanitized diagnostics. | integration | — |
| test_recall.py | 115 | test_recall_matches_nested_nap_caption | Builds two child packs and folds them into a parent, then recalls caption `pack-a`, which survives only inside the parent tree. It compares output with a line projected via private `_projected_child`, extracts its ID prefix, verifies public zoom reaches dated leaf `a1`, and ensures the parent caption `both` is absent. The fixture is a deterministic nested four-note tree with one top-level node. | integration | implementation-coupled |
| test_recall.py | 138 | test_recall_nested_caption_omits_notes_naps_and_git | Creates the same two-level pack hierarchy, recalls nested caption `pack-a`, and asserts that caption appears while `notes/`, `naps/`, and `git` do not. The test verifies that nested-caption matches preserve the user-facing abstraction boundary. The fixture is a temporary four-note store folded into two children and one parent with deterministic ordering and no monkeypatches. | integration | — |
| test_recall.py | 156 | test_recall_does_not_match_grain_day_or_prefix | Creates a two-leaf nap captioned `folded pair` and probes recall with its displayed grain, date, and a chosen ID-prefix character absent from the caption. It asserts all such metadata probes yield no caption match, proving recall indexes caption/text semantics rather than rendered metadata. The fixture derives the displayed prefix from `format_wake_line` over a deterministic one-node view. | integration | — |
| test_recall.py | 173 | test_recall_invalid_pattern_is_cli_error | Changes into a fresh temporary repository and invokes CLI recall with invalid regex `[`. It asserts a nonzero result and lowercases captured stderr to verify the error is not an invalid-command classification and leaks no notes path, naps path, or Git terminology. The fixture uses the in-process `main` entry point, pytest capture, and no existing note payloads. | integration | — |
| test_tox_runner.py | 18 | test_tox_env_list_is_non_eol_cpython_from_3_11 | Parses committed `tox.ini` with interpolation disabled, splits the `[tox] env_list`, trims empty entries, and asserts exact equality with `py311`, `py312`, `py313`, and `py314`. The test verifies the declared supported CPython runner matrix and excludes Python 3.10 or incidental environments. The fixture is the repository manifest read through a standard `ConfigParser`, not an executed tox process. | contract | — |
| test_tox_runner.py | 28 | test_tox_skips_packaging_the_project | Parses committed `tox.ini` and asserts `[testenv] package` is exactly `skip`. This locks the runner contract that test environments execute the script-oriented project without building hatchling metadata or an sdist. The fixture is only the repository's structured INI manifest loaded without interpolation; the assertion targets one explicit configuration field rather than prose. | contract | — |
| test_tox_runner.py | 33 | test_tox_commands_run_pytest_with_posargs | Parses `[testenv] commands` from `tox.ini`, tokenizes it, and asserts the first token is `pytest` and `{posargs}` is present. This verifies tox runs the intended test runner while forwarding caller-selected test arguments. The fixture is a structured committed INI file read with brace interpolation disabled so tox placeholders remain literal and available to the assertions. | contract | — |
| test_tox_runner.py | 40 | test_pytest_collects_from_tests_directory | Parses committed `pytest.ini` and asserts `[pytest] testpaths` equals exactly `tests`. The behavior under contract is default collection rooted at the suite directory, independent of an explicit command-line path. The fixture is the repository's structured pytest manifest loaded by the common interpolation-free INI helper; no pytest subprocess or filesystem mutation occurs. | contract | — |
| test_tox_runner.py | 45 | test_coverage_env_collects_summem_lcov | Reads `[testenv:coverage] commands` from `tox.ini` and asserts it includes `--cov=summem`, the parameterized LCOV destination `lcov:{env:COVERAGE_DIR:coverage}/lcov.info`, and `{posargs}` forwarding. The test verifies the dedicated coverage environment's source target, artifact location, and caller argument contract. The fixture is the parsed committed tox manifest with literal braces preserved. | contract | — |
| test_tox_runner.py | 53 | test_default_tox_commands_have_no_cov | Parses default `[testenv] commands` and `[tox] env_list`, asserting the commands contain no `--cov` token and the environment matrix contains no `coverage` member. This proves ordinary tox runs do not collect coverage implicitly and the dedicated coverage environment is opt-in. The fixture is the structured committed `tox.ini`; assertions cover both command configuration and default environment selection. | contract | — |
| test_wake_expand.py | 48 | test_under_budget_expands_right_edge_until_budget | Builds two eight-leaf packs, sets wake budget to four, calls `wake_text`, and rereads the persisted view. It asserts the directory still contains two nodes while wake emits four non-empty lines and the first line retains an `x8` pack indicator, proving in-memory right-edge expansion fills display budget without materializing splits. The fixture is a temporary store with sixteen deterministic notes folded into two packs. | integration | — |
| test_wake_expand.py | 61 | test_at_budget_does_not_expand | Creates two eight-leaf packs and sets `WAKE_LINES` to two, exactly matching view cardinality. It calls `wake_text` and asserts two lines containing captions `eight-a` and `eight-b` in order, proving at-budget rendering preserves top-level packs rather than projecting their children. The fixture is a temporary deterministic sixteen-note store folded into two persisted peer naps. | integration | — |
| test_wake_expand.py | 73 | test_native_notes_fill_budget_without_split | Creates two eight-leaf packs plus two later loose notes, sets budget four, and replaces `loads_tree` with a raising stub before calling `wake_text`. It asserts four output lines and four persisted view nodes, thereby requiring the native file rows to fill budget without loading or splitting either tree. The fixture is a mixed two-pack/two-note store with deterministic times and a collaborator-level no-call trap. | integration | over-specified-mock |
| test_wake_expand.py | 91 | test_lone_note_does_not_split | Writes a single dated note, raises wake budget to 32, and calls `wake_text`. It asserts exactly one non-empty line and exact equality with the expected dated leaf rendering, demonstrating that extra display capacity cannot split a native note. The fixture is an isolated one-note store with a deterministic midnight timestamp, seeded name generation, and monkeypatched module budget. | integration | — |
| test_wake_expand.py | 102 | test_expand_writes_nothing | Builds two eight-leaf packs, snapshots all visible note and nap payload names, sets wake budget four, and calls `wake_text`. It asserts the payload-name set is unchanged, proving display expansion is purely in memory and neither creates nor deletes store files. The fixture is a deterministic temporary store and the oracle compares the complete payload surface before and after the SUT call. | integration | — |
| test_wake_expand.py | 113 | test_missing_tree_does_not_split | Creates a two-note nap, deletes its only `.tree`, raises wake budget to four, and renders wake. It asserts exactly one output line containing caption `pair`, proving a pack with missing children data remains an opaque top-level row instead of raising or partially splitting. The fixture is a temporary store whose `.summ` survives while its associated tree is deliberately absent. | integration | — |
| test_wake_expand.py | 129 | test_malformed_tree_does_not_split | Creates a two-note nap, overwrites its `.tree` with malformed JSON, raises wake budget, and calls `wake_text`. It asserts one output line containing `pair`, proving parse failure keeps the persisted caption as an opaque row and does not expose partial children. The fixture is a deterministic temporary pack with valid summary bytes and deliberately corrupt tree bytes. | integration | — |
| test_wake_expand.py | 145 | test_unreadable_tree_does_not_split | Creates a two-note nap, monkeypatches `Path.read_bytes` to raise `PermissionError` only for its tree, and calls `wake_text` at budget four. It asserts exactly one line containing caption `pair`, proving unreadable child data degrades to an unsplit pack. The fixture preserves normal reads for all other paths and uses a targeted filesystem-boundary fault over a real persisted nap. | integration | — |
| test_wake_expand.py | 168 | test_nested_empty_nap_child_does_not_split | Creates a normal two-note nap, replaces its tree with valid serialized data containing one note and one empty nested nap, and renders at budget four. It asserts a single line containing the original `pair` caption, proving an invalid empty nested branch prevents expansion of the enclosing file. The fixture uses production tree serializers and explicit `NoteChild`/`NapChild` values in a temporary store. | integration | — |
| test_wake_expand.py | 193 | test_malformed_tree_is_loaded_at_most_once | Builds two eight-leaf packs, corrupts the right tree, wraps `loads_tree` to record every byte sequence, and renders wake at budget four. It asserts four lines are produced and the malformed byte payload is passed to the parser exactly once, verifying failed file-backed tree loads are cached within one wake operation. The fixture combines a real mixed-validity store, deterministic packs, and call recording around the real parser. | integration | — |
| test_wake_expand.py | 217 | test_expand_prints_dated_nested_notes | Writes two timestamped notes, folds them into a nap, sets budget two, and calls `wake_text`. It asserts exact ordered equality with two independently formatted dated leaf lines for `alpha` and `beta`, proving expansion projects child timestamps and text rather than the parent caption. The fixture is a deterministic two-note temporary store reduced to one persisted nap before in-memory expansion. | integration | — |
| test_wake_expand.py | 233 | test_zoom_expanded_child_id | Builds two eight-leaf packs, expands the public frontier to budget four, removes IDs already present as top-level files, and selects one projected child ID. It calls `zoom_text` for that ID but asserts only that a child ID exists and the returned string is truthy; no assertion ties the output to that selected child's known caption, children, or note text. The fixture is a deterministic nested store and public expansion result. | integration | vacuous-assertion |
| test_zipper.py | 116 | test_leaf_digests_of_note_is_its_digest | Writes one note, reads its public view node, and calls `leaf_digests`. It asserts exact set equality with the digest computed from the note file's actual bytes, proving a loose note contributes one canonical leaf digest. The fixture is an initialized temporary store with deterministic timestamp/name generation, one persisted note file, and no nap tree or malformed state. | integration | — |
| test_zipper.py | 125 | test_leaf_digests_of_nap_is_union_of_tree_digests | Writes `alpha` and `beta`, independently computes their canonical note-byte digests, folds their node IDs into one nap, and calls `leaf_digests` on the resulting view node. It asserts exact equality with the two-digest expected set, proving nap leaf identity is the union encoded by its canonical tree. The fixture is a deterministic two-note store with one persisted `.tree` and `.summ`. | integration | — |
| test_zipper.py | 138 | test_leaf_digests_none_when_tree_missing_or_malformed | Creates multiple two-leaf naps and independently subjects their trees to malformed JSON, deletion, and valid JSON lacking required tree shape. It confirms a valid nap initially has digests, then asserts `leaf_digests` returns `None` for each unavailable or invalid representation. The fixture is a temporary store with deterministic notes, three distinct corruption modes, and fresh view reads after mutation. | integration | — |
| test_zipper.py | 160 | test_two_identical_notes_stay | Writes two notes with identical text, reads both view nodes, and computes the shared content digest. It asserts each node reports the same singleton leaf set and both distinct timestamped files still exist, proving leaf-set helpers do not deduplicate physical notes merely because their content IDs collide. The fixture is a temporary store with duplicate text and deterministic but distinct sequence names. | integration | — |
| test_zipper.py | 172 | test_rematerialize_note_writes_name_and_bytes | Ensures an empty store, constructs a `NoteChild` with explicit canonical name and text, and calls `rematerialize_child`. It asserts the destination under `.summem/notes/{name}` contains exactly `note_file_bytes("alpha")`, verifying both placement and canonical serialization. The fixture has no pre-existing payload and uses a fully explicit child object rather than deriving it from a prior tree. | integration | — |
| test_zipper.py | 183 | test_rematerialize_nap_stem_uses_leftmost_seq_child_id_and_leaves | Creates and folds two notes, deserializes the resulting tree into a `NapChild`, unlinks the original node, and rematerializes it. It asserts emitted `.tree` and `.summ` bytes under a stem combining left sequence, child ID, and leaf count, then directly asserts private `_nap_stem` equals that stem. The fixture is a real two-note pack manipulated through several underscore-prefixed helpers. | integration | implementation-coupled |
| test_zipper.py | 204 | test_rematerialize_does_not_clobber_existing_dest | Prewrites a note destination with `kept`, rematerializes a same-name child containing `new`, and asserts bytes remain unchanged; then creates a nap, snapshots both payload files, rematerializes a colliding `NapChild`, and asserts both snapshots survive. It also directly asserts private `_nap_stem` equals the node name. Fixtures cover independent note and nap stores with deliberate destination collisions. | integration | implementation-coupled |
| test_zipper.py | 229 | test_heal_ab_vs_abcd_keeps_coarse_pack | Builds a balanced four-leaf parent, rematerializes its `AB` child beside it, and calls `heal_view`. It asserts the view contains only the four-leaf parent, the parent ID is preserved, no synthesized `CD` payload name appears, and both edge leaves remain reachable. The fixture is a deliberately overlapping coarse/fine store with deterministic notes and independently computed `CD` leaf-set ID. | integration | — |
| test_zipper.py | 245 | test_heal_parent_plus_children_keeps_parent | Builds a balanced four-leaf parent, rematerializes both two-leaf children beside it, and heals the view. It asserts exactly one node remains, with the original parent ID and four leaves, then verifies zoom from that parent reaches each original sentence `A` through `D`. The fixture is a three-node overlapping representation of one leaf set, constructed from canonical persisted child trees. | integration | — |
| test_zipper.py | 262 | test_heal_parent_plus_children_plus_neighbor_resplits | Builds a four-leaf parent plus both children, constructs an overlapping `CE` neighbor and an `ABCE` nap, then heals. It asserts the final store is a unique cover and every sentence `A`, `B`, `C`, `D`, and `E` remains reachable, proving redundant children are dropped before the parent is resplit around the overlap. The fixture is a hand-built five-leaf graph using real rematerialization and tree objects. | integration | — |
| test_zipper.py | 285 | test_heal_abd_vs_abe_unique_cover | Constructs overlapping packs `ABD` and `ABE` sharing `AB`, records all pre-heal summary sentences, and calls `heal_view`. It asserts a unique cover, reachability of `A`, `B`, `D`, and `E`, fewer than four loose note files, and every post-heal summary drawn from the prior summary set. The fixture is a deterministic prefix-overlap store designed to detect invented captions and O(total-leaves) note materialization. | integration | — |
| test_zipper.py | 317 | test_heal_note_covered_by_nap_dropped | Creates an `AB` nap, rematerializes its `A` leaf as a loose note, confirms a note is visible, and heals. It asserts every remaining view node is a nap, the original nap ID remains, and zoom from it still reaches `A`, proving a redundant loose leaf is removed without losing nested accessibility. The fixture is a two-leaf pack plus one exactly covered note payload. | integration | — |
| test_zipper.py | 335 | test_heal_disjoint_is_noop | Creates two disjoint two-leaf naps `AB` and `CD`, snapshots every visible payload filename, and calls `heal_view`. It asserts the payload-name set is byte-for-byte structurally unchanged and the resulting store is still a unique cover, proving healing does not rewrite already disjoint packs. The fixture is a deterministic four-note store folded into two non-overlapping peers. | integration | — |
| test_zipper.py | 351 | test_heal_odd_arity_finishes_under_iteration_cap | Plants a one-child nap and a three-child nap, wraps `list_view` to count calls and raise beyond 50, then calls `heal_view`. It asserts at most 50 view reads and confirms `solo` and `t1` remain reachable after restoring the collaborator. The fixture is an intentionally odd-arity store; termination is measured through an internal invocation cap rather than a process timeout. | integration | over-specified-mock |
| test_zipper.py | 380 | test_heal_malformed_overlapping_nap_skipped | Builds a four-leaf parent and overlapping `AB` child, corrupts the child's tree, and calls `heal_view`. It asserts both parent and malformed child tree files remain, then parses the parent and uses private `_digests_of_tree` to assert its leaves still equal digests for `A` through `D`. The fixture tests graceful skipping in a mixed valid/malformed overlapping store while preserving the coarse valid pack. | integration | implementation-coupled |
| test_zipper.py | 396 | test_heal_to_8_2_1_empty_fold_request_wake_projects | Builds disjoint grains of eight, two, and one leaves, heals, and asserts sorted view grains `[1, 2, 8]` plus an empty `fold_request` at budget two. It then renders wake at budgets two and four and asserts exactly those line counts, proving wake caps over-budget output and expands under-budget output without equal-grain folding. The fixture combines balanced packs, loose notes, and two budget mutations. | integration | — |
| test_zipper.py | 417 | test_heal_idempotent_on_disjoint_store | Creates disjoint `AB` and `CD` naps, heals once, snapshots all payload names, heals a second time, and asserts the payload set is unchanged. This verifies idempotence specifically after the store has already reached a disjoint canonical view. The fixture is a deterministic four-note temporary repository with two persisted peer packs and no malformed or overlapping children. | integration | — |
| test_zipper.py | 433 | test_heal_ignores_dot_prefixed_temp | Creates a normal two-leaf nap, writes dot-prefixed scratch bytes into the naps directory, and calls `heal_view`. It asserts the temporary file still exists and its bytes remain exactly `scratch`, proving healing ignores dot-prefixed implementation temporaries. The fixture is a valid persisted pack plus one non-payload hidden file in the same directory, allowing both survival and non-modification checks. | integration | — |
| test_zipper.py | 447 | test_same_second_notes_keep_left_child_stem | Writes four same-second notes, derives the oldest node's prefix with private `_seq_prefix`, builds `AB`, `CD`, and parent `ABCD` packs, rematerializes the parent's left child, and asserts its name begins with that prefix before healing. The fixture stresses timestamp ties with deterministic RNG suffixes and a nested pack hierarchy; the oracle depends on a private helper for the expected prefix. | integration | implementation-coupled |
| test_zipper.py | 483 | test_cli_note_and_nap_call_heal | Wraps `heal_view` with a counter, invokes CLI note twice and nap once, and asserts success plus exact cumulative counts two and three; it resets the counter, invokes wake, and asserts zero heal calls. The fixture is a temporary current store with real note and nap persistence and captured stdout. Assertions target internal CLI-to-heal dispatch frequency rather than healed store postconditions. | integration | over-specified-mock |
| test_zipper.py | 508 | test_cli_wake_on_overlapping_head_writes_nothing | Plants overlapping `ABD` and `ABE` packs, snapshots payload names, wraps `fcntl.flock`, and invokes CLI wake. It asserts success, zero flock calls, and an unchanged payload set, proving wake remains read-only and does not acquire the mutating store lock even on an overlapping head. The fixture is a deliberately noncanonical temporary store and a boundary spy that delegates any unexpected lock call. | integration | — |
| test_zipper.py | 528 | test_cli_nap_overlapping_ids_exits_1_without_concat | Plants overlapping packs, invokes CLI nap on their IDs with caption `concat-caption`, and asserts exit status one. It reads every persisted summary, asserts the requested caption was not written, and verifies the store still has a unique cover, proving invalid overlapping operands are rejected without concatenation artifacts. The fixture is a temporary current repository with real overlapping tree payloads. | integration | — |
| test_zipper.py | 546 | test_cli_note_text_inside_nap_exits_0_no_loose_note | Creates an `AB` nap, invokes CLI note with duplicate text `A`, and rereads the view. It asserts success, every remaining node is a nap, and no loose note captioned `A` survives, proving mutation-time healing absorbs content already represented by a pack. The fixture is a temporary current store with one two-leaf nap and deterministic original note bytes. | integration | — |
| test_zipper.py | 560 | test_cli_invalid_nap_caption_does_not_heal | Plants an overlapping store, snapshots all payload names, wraps `heal_view` with a counter, and invokes CLI nap with an empty caption. It asserts a nonzero result, zero heal invocations, and unchanged payload names, proving validation rejects the request without writes. The fixture supplies real overlapping IDs and uses both an observable filesystem oracle and an internal no-call interaction oracle. | integration | over-specified-mock |
| test_zipper.py | 581 | test_identical_notes_nappable_after_heal_view | Writes two identical notes, heals the view, and asserts two positional node IDs remain. It then naps those IDs with caption `twins` and asserts every resulting node is a nap, proving content-identical physical notes survive healing and remain valid operands despite sharing a digest-derived ID. The fixture is an isolated duplicate-text store with deterministic distinct filenames and real write/heal/nap operations. | integration | — |
| test_zipper.py | 593 | test_with_store_lock_blocks_and_writes_no_lock_file | Ensures a store, then executes a callback under `with_store_lock`; the callback starts a subprocess that attempts a nonblocking exclusive flock on the naps directory and enumerates all files. It asserts the probe exits with blocking code two and no file named `lock` appears, proving directory flock serialization without lock-file artifacts. The fixture crosses process boundaries using the real OS `fcntl` implementation. | integration | — |
