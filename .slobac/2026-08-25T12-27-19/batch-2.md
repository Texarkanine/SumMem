## Findings

### 1. `conftest.py` → `summem` — rotten-green

- **Location:** conftest.py → summem
- **Smell:** `rotten-green`
- **Rationale:** The `summem` pytest fixture is declared and never injected by any test in the suite; every assigned (and sibling) test calls `load_summem()` directly. That matches the dead-fixture signal: a fixture declared and never referenced, so the suite reports green while this scaffolding verifies nothing. Manifesto: https://texarkanine.github.io/slobac/taxonomy/rotten-green/
- **Prescribed remediation:** Delete the unused `summem` fixture. Keep `load_summem()` as the explicit loader. This is dead scaffold with no intent to test through the fixture.
- **Why this isn't a false positive:** This is not an explicit pending marker (`pytest.mark.skip` / `xfail`); the fixture is silently unused. The repo has no unused-fixture lint gate, so this is the semantic dead-scaffold case the linter carve-out does not already cover.

### 2. `test_cli.py` → `test_version_info_is_checked_before_import_tomllib` — implementation-coupled

- **Location:** test_cli.py → test_version_info_is_checked_before_import_tomllib
- **Smell:** `implementation-coupled`
- **Rationale:** The title claims the driver checks `sys.version_info` before `import tomllib`. The body never invokes the driver or `require_python`; it `SCRIPT.read_text()` and asserts character offsets (`import sys` then `version_info` then `import tomllib`). That is the undocumented-implementation-shape signal: a source-order pin, not an observable refusal. Manifesto: https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/
- **Prescribed remediation:** Drive the public behavior already covered by `test_driver_refuses_python_310_before_tomllib` (subprocess on CPython 3.10: exit 1, floor message, no `tomllib` / traceback). Delete this source-order test, or replace it with that subprocess contract only.
- **Why this isn't a false positive:** This is not sanctioned same-module access to a `_`-prefixed helper, nor `@VisibleForTesting`. It bypasses the public CLI/`require_python` surface and locks statement order in the committed driver file.

### 3. `test_init.py` → `test_prompt_text_invariants` — loose-text-oracle

- **Location:** test_init.py → test_prompt_text_invariants
- **Smell:** `loose-text-oracle`
- **Rationale:** The docstring claims the bootstrap teaches always-unless root wake and note, and omits the versioned how-to. The body identifies that meaning with unanchored tokens (`wake`, `root`, `note`, `conversation`, `contributor`, `personal`) plus a phrase checklist. Opposite-polarity or unrelated copy that happens to contain those tokens still passes; token presence is the primary identifier of which prompt was emitted. Manifesto: https://texarkanine.github.io/slobac/taxonomy/loose-text-oracle/
- **Prescribed remediation:** Treat the rendered prompt as the product: assert `prompt_text()` against a reviewed full golden, or parse sections and assert structured fields (title, mandatory wake instruction, note instruction, AGENT_BIN path). Keep the how-to-exclusion checks only as documented fitness-function negatives, not as the meaning lock.
- **Why this isn't a false positive:** This is not a full golden/approval of UX copy (the legitimate "text is the product" carve-out), not a typed/coded primary oracle with a supplementary datum, and not an i18n key check. It is a lone underdetermined substring checklist on runtime-emitted prompt text.

### 4. `test_init.py` → `test_prompt_text_notes_are_part_of_the_work` — loose-text-oracle

- **Location:** test_init.py → test_prompt_text_notes_are_part_of_the_work
- **Smell:** `loose-text-oracle`
- **Rationale:** The title claims `prompt_text()` treats script-written files as part of the work, not a separate git procedure. The body locks that claim with a keyword checklist (`part of your work`, `untracked`, `invent filenames`, `rewrite`, `the only writer`) and forbidden tokens (`git add`, `own commit`, `notes/`, `naps/`). Shared tokens still match copy that does not teach the work-tree rule. Manifesto: https://texarkanine.github.io/slobac/taxonomy/loose-text-oracle/
- **Prescribed remediation:** Same as the sibling invariants test: golden or structured-section asserts for the "While Working" paragraph. Do not grow the keyword list.
- **Why this isn't a false positive:** The negatives without an architectural `.because()`-style rationale are still a meaning-proxy checklist, not a schema/manifest validation and not a full presentation golden.

### 5. `test_init.py` → `test_how_to_text_is_the_usage_section` — loose-text-oracle

- **Location:** test_init.py → test_how_to_text_is_the_usage_section
- **Smell:** `loose-text-oracle`
- **Rationale:** The docstring claims `how_to_text()` is the root-wake Usage section (header, taught verbs, no runbook). After a strong `startswith("== SumMem Usage ==")` check, the body still identifies taught verbs and exclusions via unanchored tokens (`note`, `zoom`, `recall`, `clone`, `catalog`, `already stored`). Several distinct how-tos that mention those words would pass. Manifesto: https://texarkanine.github.io/slobac/taxonomy/loose-text-oracle/
- **Prescribed remediation:** Golden-snapshot the Usage section, or assert parsed paragraphs (header, note rule, nap-already-stored rule, recall/zoom grammar, catalog-vs-command rule). Drop the token spray.
- **Why this isn't a false positive:** `AGENT_BIN` and the header are real anchors, but they are not the primary identifier of "taught verbs, no runbook"; the substring checklist is. This is not a structured parse after the fact (the cured form).

### 6. `test_proof_ingest.py` — deliverable-fossils

- **Location:** test_proof_ingest.py → (module docstring / file name)
- **Smell:** `deliverable-fossils`
- **Rationale:** The file is named `test_proof_ingest.py` and the module docstring is "First proof 1: two worktrees note, merge, both notes in the view." That is checklist/work-item vocabulary (`First proof 1`), not a product-capability name, even though the test function itself is behavior-shaped. Signal: file names and docstrings mirroring the delivery breakdown / design-doc numbering. Manifesto: https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/
- **Prescribed remediation:** Phase A: rename the file to a product capability (e.g. `test_worktree_note_merge.py`) and rewrite the module docstring to that claim. Leave `test_two_worktrees_note_merge_without_conflict` as-is. Phase B regroup belongs to the cross-suite pass with the other `test_proof_*` files.
- **Why this isn't a false positive:** "Proof" here is work vocabulary (numbered first-proof checklist), not a product entity named Proof. The body is not testing a refactor-safety property; the fossil words appear only in the file/docstring, not in the SUT.

### 7. `test_proof_scopes.py` — deliverable-fossils

- **Location:** test_proof_scopes.py → (module docstring / file name)
- **Smell:** `deliverable-fossils`
- **Rationale:** Module docstring "First proofs 7 and 8: --path walk-up and root-wake catalog" and the `test_proof_*` filename organize two store-scope behaviors under sprint proof numbers. Signal: design-doc section numbers and delivery-breakdown file names. Manifesto: https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/
- **Prescribed remediation:** Phase A: rename to a capability-shaped file (e.g. `test_path_walkup_and_root_catalog.py`) and drop "First proofs 7 and 8" from the docstring. Test function names already encode the behavior; do not recycle the proof numbers.
- **Why this isn't a false positive:** `--path` and catalog are product terms, but "First proofs 7 and 8" is only work vocabulary. This is not a test of a product `proof` or `milestone` entity.

### 8. `test_proof_squash.py` — deliverable-fossils

- **Location:** test_proof_squash.py → (module docstring / file name)
- **Smell:** `deliverable-fossils`
- **Rationale:** Module docstring "First proof 4: three packs, squash onto main, clone still zooms" and the `test_proof_squash` filename are checklist archaeology. The single test already names the product claim. Signal: ticket/proof numbering in file organization. Manifesto: https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/
- **Prescribed remediation:** Phase A: rename to e.g. `test_squash_clone_zoom.py` and rewrite the module docstring to the squash-clone-zoom contract. Keep the test identifier.
- **Why this isn't a false positive:** Squash/clone/zoom are product/git vocabulary in the test name; the fossil is the "First proof 4" wrapper, which does not appear in the SUT.

### 9. `test_zoom.py` → `test_zoom_nap_of_naps_prints_two_children_not_leaves` — implementation-coupled

- **Location:** test_zoom.py → test_zoom_nap_of_naps_prints_two_children_not_leaves
- **Smell:** `implementation-coupled`
- **Rationale:** The title claims zoom of a nap-of-naps prints two child ids and captions, not the four leaf texts. The expected lines are built with `format_wake_line(m._projected_child(child), ids)`, which is the body of private `_zoom_kids`. That is `_private_method(` in the test body used as the oracle, so a broken projector agrees with zoom. Manifesto: https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/
- **Prescribed remediation:** Assert through the public wake grammar: two lines, captions `pack-a` / `pack-b`, grain prefixes `x2`, and original leaf texts (`a1`, `a2`, `b1`, `b2`) absent. Stop calling `_projected_child` to build `want`.
- **Why this isn't a false positive:** This is cross-module access to a `_`-prefixed helper, not same-module Python convention and not a sanctioned `#[cfg(test)]` / `@VisibleForTesting` escape hatch.

No findings for scope `tautology-theatre`.
No findings for scope `over-specified-mock`.
No findings for scope `prose-pin`.
No findings for scope `pseudo-tested`.
No findings for scope `vacuous-assertion`.
No findings for scope `conditional-logic`.
No findings for scope `monolithic-test-file`.
No findings for scope `naming-lies`.
No findings for scope `presentation-coupled`.
No findings for scope `shared-state`.
No findings for scope `mystery-guest`.

## Behavior Summaries


| File | Line | Test ID | Behavior | Tier | Smells Found |
|------|------|---------|----------|------|--------------|
| test_cli.py | 20 | test_note_subcommand_writes_and_wake_reads | Writes a note through the CLI and shows it on the next wake. SUT entry points: load_summem().main(['note', 'hello']) then main(['wake']). Assertion targets: both exit codes are 0; stdout contains hello and the closer You are up to speed.; the last stdout line is exactly that closer. Fixture shape: tmp git repo via init_repo, monkeypatch.chdir into it, capsys capture; no pre-seeded notes. The store is created as a side effect of note; wake reads the wait-free view without a second write. | unit | semantic-redundancy? |
| test_cli.py | 33 | test_nap_one_id_rejected | Rejects nap when only one 64-hex id is given, without treating nap as an unknown command. SUT entry point: main(['nap', 64-hex]). Assertion targets: exit code is nonzero; stderr lowercased does not contain invalid choice. Does not assert the arity message or that no nap was written. Fixture shape: empty tmp git repo, chdir, capsys. Argparse still recognizes the nap subcommand; the failure is arity, not an unknown-choice path. No notes are written first so there is no valid pair to fold. | unit | — |
| test_cli.py | 42 | test_nap_three_ids_rejected | Rejects nap when three 64-hex ids plus a caption are given, without treating nap as unknown. SUT entry point: main(['nap', id, id, id, 'caption']). Assertion targets: nonzero exit; stderr has no invalid choice. Does not lock the expected usage text or store emptiness. Fixture shape: empty tmp git repo, chdir, capsys. Nap is a known subcommand that requires exactly two ids plus a caption; three ids must not be parsed as an unknown program name. Store stays empty because the command never reaches write_nap. | unit | — |
| test_cli.py | 52 | test_nap_subcommand_writes_and_wake_reads | Folds two notes via CLI nap and wake shows only the caption. SUT entry points: main(['note', ...]) twice, list_view to collect ids, main(['nap', id_a, id_b, 'pair']), then main(['wake']) after WAKE_LINES is patched to 1. Assertion targets: nap exit 0; wake stdout contains pair, omits alpha, last line is You are up to speed. Fixture shape: tmp repo with two notes then one nap; capsys drained before wake. | unit | semantic-redundancy? |
| test_cli.py | 70 | test_path_flag_is_known_on_all_non_start_commands | Accepts --path on store commands and rejects it on start, init, and version. SUT entry points: main([cmd, '--path', '.', ...]) for wake, note, nap, zoom, recall, start, init, version. Assertion targets: wake/note/nap/recall exit 0; zoom exit is not 2 (argparse); start/init/version exit nonzero. Fixture shape: tmp repo; two notes then a nap so nap/zoom/recall have ids; chdir. start/init/version are the commands whose parsers omit --path; exit != 2 on zoom means argparse accepted the flag even if zoom has nothing useful to open. | unit | — |
| test_cli.py | 88 | test_note_without_text_fails | Rejects note with no text argument. SUT entry point: main(['note']). Assertion target: nonzero exit only. Fixture shape: empty tmp git repo and chdir; no stdout/stderr checks. note's TEXT argument is required by the operator catalog; this is a parse failure before any store write. No capsys check because the claim is only the nonzero status. | unit | — |
| test_cli.py | 95 | test_cli_nap_overlong_prints_ratchet | Over-long nap caption prints the byte ratchet and writes no nap. SUT entry points: two notes, list_view, main(['nap', ids..., caption]) with caption length ENTRY_CHARS+1. Assertion targets: exit 1; stderr contains the utf-8 length, ENTRY_CHARS, Accented characters cost 2 bytes, Compress it further; omits notes/, naps/, git; naps dir has no payload files. Fixture shape: tmp repo with two short notes; capsys drained before nap. | unit | semantic-redundancy? |
| test_cli.py | 118 | test_rejected_note_does_not_print_saved | A 281-character note exits 1 and does not print Saved. SUT entry point: main(['note', 'x'*281]). Assertion targets: exit 1; Saved. absent from stdout. Does not check the ratchet text. Fixture shape: empty tmp git repo, chdir, capsys. Saved. is the success line for a written note; a ratchet rejection must not emit it on stdout. 281 characters is ENTRY_CHARS+1 in ASCII. | unit | semantic-redundancy? |
| test_cli.py | 127 | test_note_error_text_omits_store_paths_and_git | Over-long note rejection names the 281/280 ratchet and omits store paths and git. SUT entry point: main(['note', 'x'*281]). Assertion targets: exit 1; stderr contains 281, 280, Accented characters cost 2 bytes, Compress it further; notes/, naps/, and git are absent. Fixture shape: empty tmp git repo, chdir, capsys. Agent-facing rejection copy must stay path-free so the operator is not sent into notes/ or asked to run git. 280 is the ENTRY_CHARS byte budget. | unit | semantic-redundancy? |
| test_cli.py | 142 | test_wake_prints_chinese_under_ascii_io_encoding | Wake still emits 你好 when the child process has PYTHONIOENCODING=ascii. SUT entry points: write_note(repo, '你好', fixed UTC datetime, Random(0)); subprocess.run([python, SCRIPT, 'wake'], cwd=repo, env with PYTHONIOENCODING=ascii). Assertion targets: returncode 0; stdout bytes contain UTF-8 你好. Fixture shape: tmp git repo; isolated env copy; no in-process main(). The child is a fresh interpreter so in-process stdout reconfigure cannot hide an ASCII encode error. write_note plants the CJK bytes before the subprocess wake. | integration | — |
| test_cli.py | 159 | test_refuses_python_before_311 | require_python rejects 3.10 and accepts 3.11+. SUT entry points: require_python((3, 10, 12)), require_python((3, 11, 0)), require_python((3, 12, 0)). Assertion targets: SystemExit with code 1 on 3.10; the 3.11/3.12 calls must not raise (implicit). Fixture shape: no repo; loaded module only. This exercises the callable require_python helper, not the import-time gate in the driver preamble. 3.11.0 is the inclusive floor; 3.12.0 is a newer accepted tuple. | unit | — |
| test_cli.py | 169 | test_version_info_is_checked_before_import_tomllib | Pins the committed driver source so version_info appears after import sys and before import tomllib. SUT entry point: none — SCRIPT.read_text() only. Assertion targets: the import sys marker exists; version_info offset is nonnegative and strictly before import tomllib in the suffix. Fixture shape: no repo; reads the repo-root summem file as bytes/text. tomllib is 3.11-only; the pin is meant to prove the version gate runs before that import so 3.10 fails with the floor message rather than ImportError. | unit | implementation-coupled |
| test_cli.py | 209 | test_driver_refuses_python_310_before_tomllib | On a real CPython 3.10 interpreter the driver prints the floor message instead of a tomllib ImportError. SUT entry point: subprocess.run([py310, SCRIPT, 'version']). Assertion targets: returncode 1; stderr contains SumMem needs Python 3.11 or newer; tomllib and Traceback are absent. Fixture shape: discovers 3.10 via uv python find or python3.10; pytest.skip if none; no repo required. version is a no-repo command so the child can start without init_repo. The skip path is pytest.skip with a reason when this host has no 3.10. | integration | — |
| test_cli.py | 225 | test_shebang_and_executable_bit | The repo-root driver has a python3 shebang and is user-executable. SUT entry point: none — SCRIPT.read_text() first line and SCRIPT.stat().st_mode. Assertion targets: first line is exactly #!/usr/bin/env python3; S_IXUSR is set. Fixture shape: committed file on disk. The shebang is how operators invoke ./summem without a .py suffix; the executable bit is the other half of that contract on POSIX. | unit | — |
| test_cli.py | 232 | test_cli_malformed_tree_returns_1_without_traceback | CLI nap against a nap whose .tree is not JSON exits 1 without traceback or store paths. SUT entry points: notes, nap, extra note, then main(['nap', nap.id, note.id, 'nope']) after writing {not json to nap.tree_path. Assertion targets: exit 1; stderr has unreadable pack, no Traceback, no notes/, naps/, or git. Fixture shape: tmp repo with one nap and one leftover note; capsys drained before the failing nap. | unit | semantic-redundancy? |
| test_cli.py | 255 | test_nap_accepts_unique_prefix | nap accepts unique 8-hex prefixes of two view ids and leaves a single nap. SUT entry points: write_note twice with distinct Random/stamps, short_id for each, main(['nap', pa, pb, 'pair']), list_view. Assertion targets: nap exit 0; view length 1; that node kind is nap. Fixture shape: tmp repo with alpha/beta notes at successive UTC seconds. short_id is the same helper wake prints; prefixes are unique here so they stay at the 8-hex floor. list_view after nap is the public store observation. | unit | — |
| test_cli.py | 271 | test_unknown_prefix_is_error | Unknown nap prefixes exit 1, hint to copy an id from wake, and write no nap. SUT entry point: main(['nap', 'deadbeef', 'cafebabe', 'pair']) after two real notes exist. Assertion targets: exit 1; stderr contains unknown id and Copy an id from wake; naps/*.summ glob is empty. Fixture shape: tmp repo with alpha/beta notes. deadbeef/cafebabe are well-formed 8-hex tokens that resolve_id cannot map onto the two real notes. The empty glob is the no-write half of the contract. | unit | semantic-redundancy? |
| test_cli.py | 285 | test_ambiguous_prefix_is_error | An 8-hex prefix that matches two patched view ids exits 1 and writes no nap. SUT entry points: list_view monkeypatched to two colliding ids (aabbccdd + 0/1 padding); main(['nap', 'aabbccdd', 'aabbccdd', 'pair']). Assertion targets: exit 1; stderr contains ambiguous and Give a longer prefix; no *.summ under naps. Fixture shape: tmp repo with two notes; replace() clones of those ViewNodes. The two synthetic ids share a3f2c1b8; named_ids is patched so resolve_id sees only those identities. match=ambiguous is the pytest.raises primary oracle. | unit | semantic-redundancy? |
| test_cli.py | 304 | test_nap_accepts_prefix_of_identical_notes | Two identical note texts nap via one unique prefix; fold_request does not print 64-hex ids. SUT entry points: two write_note('hello', ...) with different stamps/Random, short_id, fold_request(repo, 1), main(['nap', prefix, prefix, 'twins']). Assertion targets: ids equal; prefix length 8; full id absent from prompt; prompt contains nap {prefix} {prefix}; after nap, view is one nap. Fixture shape: WAKE_LINES patched to 1; tmp repo. | unit | — |
| test_cli.py | 326 | test_bare_invocation_prints_command_catalog | Bare main([]) prints usage_text() and documents --path only on store commands. SUT entry points: usage_text(); main([]). Assertion targets: nonzero exit; catalog appears in out+err; wake/note/nap/zoom/recall lines include --path; start/init/version lines do not. Fixture shape: no repo; capsys. usage_text() is the oracle for the printed catalog; main([]) is the missing-command path. Lines are matched by the substring summem <name>. | unit | semantic-redundancy? |
| test_cli.py | 346 | test_help_flag_prints_catalog | -h and --help print the catalog and exit 0. SUT entry points: usage_text(); main(['-h']) and main(['--help']). Assertion targets: exit 0; catalog in out+err; stdout nonempty; catalog mentions PWD, --path, and file or directory. Fixture shape: no repo; loop over both flags with capsys each time. Both short and long help must be the same catalog, not argparse's default usage. PWD and file/directory document how --path walks. | unit | semantic-redundancy? |
| test_cli.py | 361 | test_help_before_command_prints_command_help | -h wake prints wake help including --path, not the top-level choice list. SUT entry point: main(['-h', 'wake']). Assertion targets: exit 0; --path in out+err; {wake,note is absent. Fixture shape: no repo; capsys. Putting -h before the subcommand is the argparse pattern that would otherwise print only the top-level {wake,note,...} list. | unit | — |
| test_cli.py | 371 | test_command_help_still_shows_path | wake -h still lists --path. SUT entry point: main(['wake', '-h']). Assertion targets: exit 0; --path in out+err. Fixture shape: no repo; capsys. wake is a store command; its dedicated -h output must still advertise --path so agents do not think the flag is top-level only. | unit | semantic-redundancy? |
| test_cli.py | 380 | test_start_help_omits_path | start -h does not list --path. SUT entry point: main(['start', '-h']). Assertion targets: exit 0; --path absent from out+err. Fixture shape: no repo; capsys. start takes a positional directory and creates a store there; --path would duplicate that role and is not on the parser. | unit | — |
| test_cli.py | 389 | test_catalog_omits_store_paths_and_git | The command catalog omits notes/, naps/, and git while still showing --path and summem wake. SUT entry points: usage_text(); main([]). Assertion targets: nonzero exit; combined catalog+capture has no notes/, naps/, or git (case-insensitive); catalog contains --path and summem wake. Fixture shape: no repo; capsys. The catalog is operator/agent help; naming notes/, naps/, or git would leak store layout the CLI otherwise hides. | unit | semantic-redundancy? |
| test_cli.py | 403 | test_wake_without_repository_errors | wake outside a git repository exits 1, names repository, avoids git, and creates no store. SUT entry point: main(['wake']) after chdir to a non-repo tmp_path. Assertion targets: exit 1; stderr lowercased contains repository and not git; tmp_path/.summem does not exist. Fixture shape: bare tmp directory, not init_repo. resolve_parent requires a git work tree; the word repository is the allowed vocabulary instead of git. Creating .summem here would strand a store outside version control. | unit | semantic-redundancy? |
| test_cli.py | 414 | test_start_without_repository_errors | start outside a repository exits 1 and does not create a store. SUT entry point: main(['start', 'pkg']) in a non-repo cwd. Assertion targets: exit 1; stderr contains repository, not git; pkg/.summem is absent. Fixture shape: bare tmp_path; chdir. start must not mkdir a store that cannot be committed. The pkg argument is only a relative directory name under the non-repo cwd. | unit | semantic-redundancy? |
| test_cli.py | 425 | test_help_without_repository_still_prints_catalog | -h still prints the catalog when cwd is not a repository. SUT entry point: main(['-h']) after chdir to tmp_path. Assertion targets: exit 0; stdout contains summem wake and --path. Fixture shape: non-repo tmp directory. Help must not depend on resolve_parent, so a missing repository cannot block -h. This is the no-repo counterpart of the catalog tests. | unit | — |
| test_cli.py | 435 | test_script_is_repo_root_driver | The test loader points at the committed repo-root summem file. SUT entry point: none — compares conftest.SCRIPT to ROOT/summem. Assertion targets: equality and SCRIPT.is_file(). Fixture shape: repository paths only; suite-bootstrap smoke. conftest.SCRIPT and ROOT are the same paths every in-process test loads via SourceFileLoader; this is the suite wiring check. | unit | — |
| test_cli.py | 441 | test_catalog_omits_store_driver_path | usage_text names the operator binary summem, not .summem/summem. SUT entry point: usage_text(). Assertion targets: .summem/summem absent; every wake line starts with summem wake after lstrip. Fixture shape: loaded module only. Agents are taught .summem/summem in the baked prompt; the operator catalog must keep the unsuffixed summem name. | unit | — |
| test_cli.py | 451 | test_unknown_token_does_not_write_a_note | A non-command token is argparse invalid choice and writes no note; a later note still works. SUT entry points: main(['raw invocation of random stuff']) then main(['note', 'ok']). Assertion targets: first exit nonzero; stderr has invalid choice; notes glob empty; after real note, a non-dot file exists. Fixture shape: tmp git repo; chdir. A leftover raw-invocation path must not fall through to note. The follow-up note ok proves the store can still write after the rejection. | unit | — |
| test_cli.py | 466 | test_cli_zoom_range_token_is_not_a_content_id | CLI zoom of a range token names the token and tells the agent to copy an id from wake. SUT entry point: main(['zoom', '16-31']). Assertion targets: exit 2; stderr contains not a content id: 16-31 and Copy an id from wake. Fixture shape: empty tmp git repo. 16-31 looks like a wake grain range, not a leaf-set id; argparse accepts zoom's operand but resolve_id must refuse it as a content id. | unit | — |
| test_cli.py | 476 | test_cli_zoom_malformed_tree_returns_1 | CLI zoom of a nap whose .tree is not JSON exits 1 with unreadable pack and no traceback or paths. SUT entry points: two write_note, write_nap, corrupt tree_path, main(['zoom', nap.id]). Assertion targets: exit 1; stderr has unreadable pack; no Traceback, notes/, naps/, or git. Fixture shape: tmp repo with one two-note nap. The nap remains in the view so zoom finds the id, then loads_tree fails. CLI must map that to exit 1 and the unreadable pack phrase. | unit | semantic-redundancy? |
| test_cli.py | 497 | test_cli_zoom_oserror_returns_1 | CLI zoom exits 1 with unreadable pack when reading the nap .tree raises OSError. SUT entry points: write two notes and a nap; Path.read_bytes monkeypatched to raise OSError for that tree path; main(['zoom', nap.id]). Assertion targets: exit 1; same agent-safe stderr contract as the malformed-JSON sibling. Fixture shape: tmp repo; patch delegates other paths to the real read_bytes. OSError is the other half of _TREE_PARSE_ERRORS alongside JSON/ValueError; the agent-visible result must match the malformed-JSON path. | unit | semantic-redundancy? |
| test_cli.py | 528 | test_cli_zoom_nested_id_skips_sibling_bad_tree | Zoom of a nested leaf id still prints that leaf when another view nap has a bad tree, and warns once. SUT entry points: two naps (ab, cd); child_id from leafset of second tree's first note; first.tree_path corrupted; main(['zoom', child_id]). Assertion targets: exit 0; stdout contains that child text; stderr is exactly skipped a pack plus newline; no paths, git, or Traceback. Fixture shape: four notes folded into two naps. | unit | semantic-redundancy? |
| test_codec.py | 12 | test_note_file_bytes_appends_newline | Note file bytes are UTF-8 of the text plus one trailing newline. SUT entry point: note_file_bytes('hello'). Assertion target: exact b'hello\n'. Fixture shape: loaded module only; no repo. The trailing newline is part of the hashed identity, not just pretty printing. hello is ASCII so UTF-8 encoding is a no-op besides the newline. | unit | — |
| test_codec.py | 18 | test_note_digest_is_sha256_of_file_bytes | note_digest is lowercase hex SHA-256 of those file bytes. SUT entry points: note_file_bytes('hello'); note_digest(raw). Assertion target: equals hashlib.sha256(raw).hexdigest(). Fixture shape: loaded module; stdlib hashlib as independent oracle. The digest is of the on-disk encoding (text plus newline), not of the raw caption string. hashlib is the independent spec oracle. | unit | — |
| test_codec.py | 25 | test_leafset_id_singleton_hashes_hex_ascii | A singleton leaf-set id is SHA-256 of that digest's hex as ASCII. SUT entry points: note_digest(note_file_bytes('hello')); leafset_id([digest]). Assertion target: hashlib.sha256(digest.encode('ascii')).hexdigest(). Fixture shape: loaded module; hashlib oracle. A one-note nap id is not the note digest itself; it hashes the 64-hex digest characters as ASCII. That keeps leaf-set ids in the same hex alphabet. | unit | — |
| test_codec.py | 32 | test_leafset_id_sorts_and_concatenates_without_delimiter | Two digests sort as ASCII, concatenate with no delimiter, then SHA-256; order of inputs does not matter; a comma join is a different id. SUT entry points: note_digest of alpha and beta; leafset_id([a,b]) and leafset_id([b,a]). Assertion targets: both equal sha256 of ''.join(sorted((a,b))); neither equals the comma-joined hash. Fixture shape: loaded module. Sorted concatenation without a delimiter is the specified combiner; a comma would be a different protocol and must not collide. Input order independence is the sort. | unit | — |
| test_codec.py | 45 | test_leafset_id_hashes_utf8_chinese_file_bytes | Chinese notes hash as UTF-8 file bytes, not JSON \uXXXX escapes. SUT entry points: note_file_bytes('你好'); note_digest; leafset_id([digest]). Assertion targets: file bytes are UTF-8 plus newline; digest matches sha256 of those bytes and not of ASCII \u4f60\u597d\n; singleton leafset matches sha256 of hex digest. Fixture shape: loaded module; literal 你好. JSON dumps with ensure_ascii would emit \uXXXX; the codec must not. The singleton leafset still hashes the hex digest, not the CJK bytes again. | unit | — |
| test_codec.py | 57 | test_dumps_tree_one_note_exact_bytes | One note child dumps to canonical JSON with a trailing newline. SUT entry points: Tree(kids=[NoteChild(name=..., text='hello')]); dumps_tree(tree). Assertion target: exact compact JSON bytes with sorted keys, type note, and trailing newline. Fixture shape: in-memory Tree only; name 20260101T000000Z-aaaaaaaaaaaaaaaa. Canonical form is sort_keys, compact separators, type tag, and a trailing newline. The long name is a realistic stamp-rand filename. | unit | — |
| test_codec.py | 67 | test_dumps_tree_keeps_chinese_not_uescaped | Canonical JSON keeps UTF-8 characters and does not emit \uXXXX. SUT entry point: dumps_tree(Tree with NoteChild text 你好). Assertion targets: UTF-8 你好 in raw; b'\u4f60' absent; trailing newline; json.loads of the stripped text has c[0].text == 你好. Fixture shape: in-memory Tree name n1. ensure_ascii=False is the dumps_tree contract. json.loads after decode proves the payload is still valid JSON, not only a byte substring. | unit | — |
| test_codec.py | 79 | test_dumps_tree_nested_nap_exact_bytes | A nap child's id is the leaf-set of the original notes in its nested tree, and dumps_tree emits that exact canonical JSON. SUT entry points: note_digest/leafset_id for alpha+beta; Tree of two NoteChildren wrapped in NapChild; dumps_tree(outer). Assertion target: exact UTF-8 JSON bytes including the computed nid and nested c array. Fixture shape: in-memory trees only. The outer child is type nap with id/sum/tree; inner children are type note. nid is computed the same way write_nap would assign the pack id. | unit | — |
| test_codec.py | 101 | test_loads_tree_round_trip | loads_tree(dumps_tree(t)) round-trips a mixed note plus nested nap tree including 你好. SUT entry points: dumps_tree(outer); loads_tree(...). Assertion target: reconstructed Tree equals the original dataclass (note gamma plus nap of alpha/你好). Fixture shape: in-memory inner/outer Tree objects; no repo. Equality is dataclass equality of Tree/NoteChild/NapChild, including nested kids order and 你好 text. dumps then loads is the only I/O. | unit | — |
| test_codec.py | 121 | test_loads_tree_ignores_unknown_fields | loads_tree ignores unknown keys including leftover v and kids beside c. SUT entry point: loads_tree(json.dumps(payload with extra, v, kids, noise)). Assertion target: result equals Tree with one NoteChild n1/hello. Fixture shape: hand-built JSON object encoded with sort_keys and compact separators plus newline. extra on the child, v, leftover kids, and noise must not change the reconstructed NoteChild. Compact JSON plus newline matches dumps_tree framing. | unit | — |
| test_codec.py | 135 | test_loads_tree_rejects_kids_key_without_c | loads_tree of kids/v without c raises. SUT entry point: loads_tree(b'{"kids":[],"v":1}\n'). Assertion target: pytest.raises((KeyError, ValueError)). Fixture shape: no repo. Pre-canonical trees used kids/v; the reader must not treat kids as c. Either KeyError or ValueError is accepted because the public contract is raise, not a specific type. | unit | — |
| test_codec.py | 142 | test_loads_tree_rejects_child_missing_type | loads_tree of a child object without type raises ValueError. SUT entry point: loads_tree of c array with name and text only. Assertion target: pytest.raises(ValueError). Fixture shape: no repo. type is required to distinguish note from nap. A name+text object must not be silently accepted as a note. | unit | — |
| test_codec.py | 149 | test_loads_tree_rejects_unknown_type | loads_tree of a child with type pack raises and does not become a nap. SUT entry point: loads_tree of a nap-shaped object with type pack. Assertion target: pytest.raises(ValueError). Fixture shape: no repo. pack is a stale/wrong type tag; accepting it as nap would hide a corrupt children file. The object otherwise looks like a nap (id, sum, tree). | unit | — |
| test_init.py | 10 | test_init_prints_recipe_and_prompt | main(['init']) exits 0 and prints the insert recipe plus prompt_text(). SUT entry points: prompt_text(); main(['init']). Assertion targets: exit 0; stdout contains AGENTS.md and the full prompt; omits docs/agents-prompt.md, paste, and AGENTS.md or CLAUDE.md; prompt is nonempty. Fixture shape: no repo; capsys. The recipe tells the operator to insert the block at the top of AGENTS.md and optionally @-include it from CLAUDE.md. prompt_text() is inlined in full, not summarized. | unit | semantic-redundancy? |
| test_init.py | 24 | test_usage_init_line_does_not_say_paste | The usage_text init catalog line does not tell the operator to paste. SUT entry point: usage_text() split into lines containing CLI_NAME init. Assertion targets: at least one such line; paste absent from the first (case-insensitive). Fixture shape: loaded module only. Earlier copy told operators to paste the prompt; the catalog must not revive that verb. CLI_NAME is summem in this build. | unit | — |
| test_init.py | 36 | test_init_outside_repository_writes_nothing | init outside a repository exits 0 and creates neither a store nor AGENTS.md. SUT entry point: main(['init']) after chdir to tmp_path. Assertion targets: exit 0; tmp_path/.summem and tmp_path/AGENTS.md do not exist. Fixture shape: non-repo tmp directory; capsys drained. init prints a prompt; it is not start and must not create .summem or write AGENTS.md even when cwd is writable. | unit | — |
| test_init.py | 46 | test_init_rejects_extra_args | init with an extra token exits nonzero. SUT entry point: main(['init', 'x']). Assertion target: nonzero exit only. Fixture shape: no repo. init takes no operands; an extra token is a parse error. No help-text assertion; status is the whole claim. | unit | — |
| test_init.py | 52 | test_init_rejects_path_flag | init --path is rejected and init -h does not list --path. SUT entry points: main(['init', '--path', '.']); main(['init', '-h']). Assertion targets: first exit nonzero; help exit 0; --path absent from help out+err. Fixture shape: no repo; capsys drained between calls. init is listed with version as a non-store command; --path on init must fail the same way the catalog promises. | unit | — |
| test_init.py | 63 | test_help_before_init_prints_init_help | -h init prints init help, not top-level-only usage. SUT entry points: usage_text(); main(['-h', 'init']). Assertion targets: exit 0; stdout nonempty; {wake,note absent; stripped catalog not contained in the help text. Fixture shape: no repo; capsys. -h init is command-scoped help; embedding the full top-level catalog would hide that init has no --path and no store walk. | unit | — |
| test_init.py | 75 | test_prompt_text_invariants | prompt_text() is treated as the bootstrap: starts with # Project Memory, mentions summem/wake/root/project-root/conversation/contributor/personal/note and do not run it again, includes .summem/summem, and excludes how-to/runbook phrases. SUT entry point: prompt_text(). Assertion targets: startswith plus a long in/not-in checklist (Usage header, see and follow, you are up to speed, clone, x1 YYYY-MM-DD, prior **root** wake, etc.). Fixture shape: loaded module; no repo. | unit | loose-text-oracle, semantic-redundancy? |
| test_init.py | 104 | test_prompt_text_notes_are_part_of_the_work | prompt_text() teaches that script-written files are part of the work and forbids a separate git procedure. SUT entry point: prompt_text(). Assertion targets: lowercase contains part of your work, untracked, invent filenames, rewrite, the only writer; omits git add, own commit, the tool manages them, notes/, naps/. Fixture shape: loaded module only. The While Working paragraph is the only place this rule is taught; the test is a presence/absence checklist on that paragraph's wording. | unit | loose-text-oracle, semantic-redundancy? |
| test_init.py | 121 | test_agents_md_starts_with_prompt_text | This repo's AGENTS.md starts with prompt_text() so the shipped prompt cannot drift. SUT entry points: prompt_text(); Path(ROOT, 'AGENTS.md').read_text(). Assertion target: agents.startswith(prompt.strip()). Fixture shape: committed AGENTS.md plus generated prompt string; lockstep contract. AGENTS.md may grow extra sections after the prompt; only the prefix must stay lockstep with prompt_text() so init cannot drift from the shipped file. | unit | — |
| test_init.py | 129 | test_how_to_text_is_the_usage_section | how_to_text() is the root-wake Usage section: header, taught verbs, no runbook or extra catalogs. SUT entry points: how_to_text(); AGENT_BIN. Assertion targets: startswith == SumMem Usage ==; endswith newline; AGENT_BIN present; token checklist for note/zoom/recall/clone/wake --path/catalog and many forbidden strings (git, notes/, Run:, other catalog headers, wake --path pkg). Fixture shape: loaded module only. | unit | loose-text-oracle, semantic-redundancy? |
| test_init.py | 158 | test_how_to_text_is_not_operator_help | how_to_text() is not usage_text(); agent bin versus operator catalog name. SUT entry points: how_to_text(); usage_text(). Assertion targets: the two strings differ; .summem/summem is in how_to only; usage contains summem but not .summem/summem. Fixture shape: loaded module only. AGENT_BIN is .summem/summem (what agents run); CLI_NAME is summem (what operators type). The two strings must stay distinct documents. | unit | — |
| test_proof_ingest.py | 20 | test_two_worktrees_note_merge_without_conflict | Two worktrees each note once; merges into main have zero conflicts; wake shows both dated leaves. SUT entry points: subprocess SCRIPT note in each worktree; git add/commit; git merge --no-edit wt-a and wt-b; SCRIPT wake on main. Assertion targets: note and merge returncodes 0; no conflict markers in any .summem file; wake stdout has alpha and beta, closer You are up to speed., and the memory lines equal dated_leaf of each note file. Fixture shape: git worktrees wt-a/wt-b off init_repo(main). | integration | deliverable-fossils, wrong-level? |
| test_proof_scopes.py | 29 | test_note_path_lands_in_started_store_else_ancestor | note --path writes into a started store, else the next ancestor, and does not create an unstarted mid-path store. SUT entry points: subprocess SCRIPT start foo/packages/baz; note --path foo/packages/baz/fee.ts child; note --path foo/packages/other/x.ts rootish. Assertion targets: exactly one note file in baz/.summem with text child\n; exactly one root .summem note with rootish\n; other/.summem absent. Fixture shape: tmp git repo; nested unstarted directories. | integration | deliverable-fossils, semantic-redundancy? |
| test_proof_scopes.py | 50 | test_root_wake_lists_other_stores_pull_prints_only_that_store | Root wake lists other stores and Usage; wake --path prints only that store. SUT entry points: start pkg; note root-note; note --path pkg pkg-note; wake; wake --path pkg. Assertion targets: root stdout has root-note, ./pkg, both == headers; catalog slice omits summem wake --path pkg and .summem/summem; path wake has pkg-note, omits root-note, wake --path pkg, and Usage. Fixture shape: tmp repo with root note plus started pkg store. | integration | deliverable-fossils, semantic-redundancy? |
| test_proof_squash.py | 14 | test_three_packs_squash_clone_zooms_originals | 100 notes folded as 64/32/4, squashed onto main; a clone's wake shows three grain lines and zoom reaches one original per pack; packed commit subjects are gone. SUT entry points: write_note+git commit x100; fold_ids for three ranges; merge --squash; git clone; wake_text(clone); zoom_reaches on each nap id. Assertion targets: 100 note ids; squash exit 0; three nonempty wake lines starting x64/x32/x4; zoom reaches n000/n064/n096; clone log omits n000/n040/n070/n099. Fixture shape: WAKE_LINES patched to 3; packed branch then squash on main. | integration | deliverable-fossils, wrong-level? |
| test_surgery.py | 70 | test_contains_unique_nested_note | locate_note --contains matching one nested NoteChild returns that filename. SUT entry point: locate_note(m, repo, contains=secret, name=None) after _balanced_4 of four notes. Assertion target: equals paths[0].name. Fixture shape: tmp repo; secret sentinel-surgery-secret-aa11 plus three keep-* notes packed as ab/cd/abcd. --contains matches NoteChild text, not nap captions. The secret is unique in the zipper so locate_note does not demand a filename. | unit | — |
| test_surgery.py | 80 | test_filename_locates_nested_note | A unique note filename or seq prefix locates a nested NoteChild. SUT entry points: locate_note(..., name=paths[2].name) and locate_note(..., name=_seq_prefix(paths[2].name)). Assertion targets: both return paths[2].name. Fixture shape: _balanced_4 keep-a..d; uses private _seq_prefix to derive the prefix input. name= may be the full on-disk filename or a unique prefix of the stamp-rand order key. The third note (keep-c) is nested under the cd pack. | unit | implementation-coupled? |
| test_surgery.py | 91 | test_contains_duplicate_text_requires_filename | --contains that matches two identical notes raises until a filename is given. SUT entry points: locate_note(contains='same-line', name=None) then locate_note(name=paths[1].name). Assertion targets: pytest.raises(ValueError) on the first; second returns paths[1].name. Fixture shape: two loose notes with identical text same-line. Two loose notes share the same caption so contains is ambiguous. Filename addressing picks the second file without deleting the first (this test only locates). | unit | — |
| test_surgery.py | 102 | test_contains_nap_caption_only_is_not_found | A substring that hits only a nap caption is not a delete target. SUT entry point: locate_note(contains='SECRET_CAPTION_ONLY', name=None) after write_nap with that caption. Assertion target: pytest.raises(ValueError). Fixture shape: two notes alpha/beta folded under that caption. locate_note must not treat a nap .summ caption as a raw-note hit; otherwise surgery would target the wrong object. | unit | — |
| test_surgery.py | 114 | test_unknown_target_errors | A missing note raises ValueError and does not change the store. SUT entry point: locate_note(contains='no-such-sentence', name=None). Assertion targets: pytest.raises(ValueError); _payload_names(repo) unchanged. Fixture shape: one keep-me note; names snapshotted before the call. _payload_names is the set of notes/ and naps/ filenames excluding dotfiles. The raise is the only mutation barrier asserted. | unit | — |
| test_surgery.py | 157 | test_excise_loose_note | A loose note is unlinked; the sibling remains; heal still covers unique leaves. SUT entry point: excise_note(m, repo, paths[0].name). Assertion targets: chain == [target name]; target file gone; sibling file remains; assert_unique_cover; secret not reachable and not in recall_text; keep-sibling still reaches. Fixture shape: two loose notes including sentinel-loose-secret-cc33. Loose means the note is already a view file, so the rematerialize chain is just that filename. recall_text is the public search surface besides reaches(). | unit | — |
| test_surgery.py | 174 | test_excise_nested_note_unzips_then_unlinks | A nested target is rematerialized to notes/, unlinked, and gone from remaining trees. SUT entry point: excise_note on paths[0].name after _balanced_4. Assertion targets: chain ends with target and includes parent.name; target file gone; _tree_embeds false; unique cover; secret not reachable/recalled; keep-b/c/d still reach. Fixture shape: four-note balanced zipper; secret sentinel-nested-secret-dd44. | unit | — |
| test_surgery.py | 197 | test_excise_overlapping_packs_clears_remaining_trees | Every view nap that embeds the target is split before unlink. SUT entry point: excise_note on kids[0].name after planting overlapping ab and abcd naps. Assertion targets: chain ends with target name and length >= 3; secret not in any .tree; unique cover; secret unreachable; keep-b and keep-d still reach. Fixture shape: four notes unlinked via _unlink_node then _plant_nap of kids[:2] and kids. ab covers the first two notes; abcd covers all four, so the secret lives in two trees at once. Planting uses rematerialize_child after clearing the healed view. | unit | implementation-coupled? |
| test_surgery.py | 224 | test_excise_does_not_call_write_nap | Excise never invents a caption via write_nap. SUT entry point: excise_note with write_nap monkeypatched to raise AssertionError. Assertion targets: the call completes; secret no longer reaches. Fixture shape: _balanced_4 nested secret; tripwire on the module write_nap collaborator (documented never-call contract). rematerialize_child plus heal_view must rebuild siblings without inventing new captions through write_nap. The boom patch is the never-call contract from excise_note's docstring. | unit | — |
| test_surgery.py | 242 | test_dry_run_prints_chain_and_writes_nothing | Dry-run returns the rematerialize chain and does not change store bytes. SUT entry point: excise_note(..., dry_run=True). Assertion targets: chain ends with target and includes parent.name; _store_bytes equal before/after; tree still embeds secret; reaches still true. Fixture shape: _balanced_4; full store byte snapshot. dry_run returns the same chain plan_break_out would apply, including the target filename, then returns before unlink/heal. | unit | semantic-redundancy? |
| test_surgery.py | 260 | test_dry_run_unknown_writes_nothing | Dry-run of a missing note still errors and writes nothing. SUT entry point: excise_note(repo, 'no-such-note-name', dry_run=True). Assertion targets: pytest.raises(ValueError); _store_bytes unchanged. Fixture shape: one keep-me note; byte snapshot. The unknown-name check runs before any rematerialize. Byte equality includes notes and naps file contents, not just names. | unit | — |
| test_surgery.py | 272 | test_identical_text_deletes_only_named_file | Filename addressing drops one of two identical notes and keeps the other. SUT entry point: excise_note(paths[0].name). Assertion targets: first file gone; second remains; reaches('same-line') and recall_text still contain same-line. Fixture shape: two loose notes with identical text. Content-addressed identity would otherwise make both files look like one leaf; filename addressing is what keeps the sibling. | unit | — |
| test_surgery.py | 285 | test_main_contains_excises | main(['--contains', sentence]) excises that nested note from cwd's store. SUT entry point: main(['--contains', secret]) after chdir. Assertion targets: exit 0; stdout includes parent.name and target name on the last line; secret unreachable and not embedded; unique cover. Fixture shape: _balanced_4; capsys. main chdirs into the repo so resolve_parent uses cwd. The last stdout line is the unlinked note name after the rematerialize chain. | unit | semantic-redundancy? |
| test_surgery.py | 304 | test_main_dry_run | main(['--dry-run', '--contains', sentence]) prints the chain and writes nothing. SUT entry point: main with those flags. Assertion targets: exit 0; parent.name in stdout lines; last line is the target filename; store bytes unchanged; secret still reaches. Fixture shape: _balanced_4; byte snapshot; chdir. CLI --dry-run is the same as excise_note(dry_run=True) plus printing each chain name. Store bytes include every non-dot notes/naps file. | unit | semantic-redundancy? |
| test_surgery.py | 324 | test_main_path_flag | --path aims at a started store the same way resolve_parent does. SUT entry points: summem main(['start', 'pkg']); write_note into repo/pkg; surgery main(['--path', 'pkg', '--contains', secret]). Assertion targets: exit 0; note file under pkg/.summem/notes gone; secret does not reach in pkg. Fixture shape: tmp repo with a started pkg store; chdir at repo root. start pkg creates pkg/.summem; --path pkg must resolve there rather than the repo-root store. write_note plants the secret only in pkg. | unit | — |
| test_surgery.py | 338 | test_main_usage_without_target | Neither --contains nor a name is usage (exit 2). SUT entry point: main([]). Assertion targets: exit 2; stderr is nonempty. Fixture shape: tmp git repo; chdir; no usage-text equality. Exit 2 is argparse/usage; stderr is the _usage() blurb. Neither --contains nor a positional NAME was supplied. | unit | — |
| test_surgery.py | 348 | test_main_version_prints_script_version | main(['version']) exits 0 and prints surgery.__version__ plus a newline. SUT entry point: main(['version']). Assertion targets: exit 0; stdout == f'{s.__version__}\n'. Fixture shape: no repo; capsys. surgery.__version__ is the x-release-please-version marker; stdout is exactly that string plus newline with no extra banner. | unit | semantic-redundancy? |
| test_surgery.py | 355 | test_main_version_outside_repository | version outside a repository exits 0 and creates no store. SUT entry point: main(['version']) after chdir to tmp_path. Assertion targets: exit 0; same version line; tmp_path/.summem absent. Fixture shape: non-repo tmp directory. version is parsed before resolve_parent, so a missing git repo cannot fail the command or mkdir .summem. | unit | semantic-redundancy? |
| test_surgery.py | 364 | test_main_version_rejects_extra_args | version with an extra token exits nonzero. SUT entry point: main(['version', 'x']). Assertion target: nonzero exit. Fixture shape: no repo. version is a lone token; any follower is treated as usage/nonzero, matching summem's version extra-arg rule. | unit | — |
| test_surgery.py | 370 | test_main_prints_fold_request_when_over_budget | After excision over WAKE_LINES, stdout includes fold_request so an agent can start the nap cascade. SUT entry point: main(['--contains', secret]) with config WAKE_LINES=2. Assertion targets: exit 0; target name in stdout lines; Compress these two... 280 characters present; Run: .summem/summem nap present; remaining notes are 3; nap short ids appear; a pre-excise id is absent. Fixture shape: four loose notes; config.toml written under .summem. | unit | semantic-redundancy? |
| test_surgery.py | 394 | test_main_path_fold_request_is_copy_paste_safe | After --path excision over budget, the Run: line naps that store from $PWD. SUT entry points: start pkg; write four notes in pkg; surgery main(['--path', 'pkg', '--contains', secret]); then summem main(tokens[1:]) from the shlex-split Run: line with last token replaced by pair. Assertion targets: surgery exit 0; pkg view has a nap captioned pair. Fixture shape: started pkg store; WAKE_LINES=2 config; chdir repo root. | unit | semantic-redundancy? |
| test_surgery.py | 414 | test_main_dry_run_omits_fold_request | Dry-run does not print a fold request; the store is unchanged. SUT entry point: main(['--dry-run', '--contains', secret]) with WAKE_LINES=2. Assertion targets: exit 0; stdout omits Compress these two and Run: .summem/summem nap. Fixture shape: four loose notes plus config.toml; does not re-check store bytes here. fold_request is only appended after a real excision when the view is over WAKE_LINES. Dry-run must not tell the agent to nap. | unit | semantic-redundancy? |
| test_zoom.py | 22 | test_zoom_two_note_nap_prints_both_texts | Zoom of a two-note nap prints both original texts as dated leaves. SUT entry points: write_note alpha/beta; write_nap; zoom_text(repo, nap_id). Assertion targets: exactly two lines; line0/1 equal dated_leaf stamps 20260101T000001Z/000002Z with alpha/beta. Fixture shape: tmp repo via _two_notes helper. dated_leaf is the x1 YYYY-MM-DD: text grammar. Stamps come from the write_note clocks (second 1 and 2 on 2026-01-01 UTC). | unit | — |
| test_zoom.py | 36 | test_zoom_conflict_sum_still_prints_leaves | Conflict markers in the parent .summ do not affect zoom of the leaves. SUT entry points: write_nap then overwrite *.summ with conflict markers; zoom_text(repo, nap_id). Assertion targets: alpha and beta appear in the output (not full dated-line equality). Fixture shape: two-note nap; first .summ file rewritten. Zoom reads .tree, not the conflicted .summ caption, so HEAD/other markers must not appear as the zoom contract. | unit | — |
| test_zoom.py | 50 | test_zoom_loose_note_id_prints_the_note | Zoom of a loose-note id prints that note as a dated leaf. SUT entry points: write_note('hello', midnight UTC, Random(0)); zoom_text(repo, cid). Assertion target: exact dated_leaf('20260101T000000Z', 'hello') plus newline. Fixture shape: single loose note. A loose note's view id is the singleton leaf-set of its file bytes. Random(0) at midnight UTC yields stamp 20260101T000000Z. | unit | — |
| test_zoom.py | 59 | test_zoom_unknown_id_omits_store_paths_and_git | Unknown zoom id is rejected without mentioning notes/, naps/, or git. SUT entry point: zoom_text(repo, '0'*64). Assertion targets: ValueError; message contains unknown id and Copy an id from wake; omits notes/, naps/, git. Fixture shape: empty tmp git repo. 64 zero hex is well-formed but not in named_ids. The wake-copy hint is only for this unknown-id class. | unit | semantic-redundancy? |
| test_zoom.py | 73 | test_zoom_missing_tree_unknown_id_has_no_wake_hint | Zoom of a view nap with no .tree says unknown id and does not say to copy from wake. SUT entry point: zoom_text after unlink of nap.tree_path. Assertion targets: ValueError with unknown id; Copy an id from wake absent. Fixture shape: two-note nap whose children file was deleted. A view nap whose .tree was unlinked cannot be zoomed; telling the agent to copy the same id from wake would loop. | unit | — |
| test_zoom.py | 88 | test_zoom_nap_of_naps_prints_two_children_not_leaves | Zoom of a nap-of-naps prints two child ids and captions, not all original texts. SUT entry points: four notes, two naps, parent nap both; zoom_text(parent.id). Assertion targets: two lines equal format_wake_line(_projected_child(child), ids) for each tree kid; each child short_id length is 8. Fixture shape: a1/a2/b1/b2 packed as pack-a/pack-b then both. Oracle uses private _projected_child. One-level zoom must stop at pack-a and pack-b captions/ids, not explode into a1/a2/b1/b2. The 8-hex check is that those child ids are unique at the floor. | unit | implementation-coupled |
| test_zoom.py | 110 | test_zoom_accepts_unique_prefix | zoom accepts a unique prefix of a pack id. SUT entry points: write_nap; short_id(nap_id, named_ids); zoom_text(repo, prefix). Assertion targets: output contains alpha and beta. Fixture shape: two-note nap. short_id against named_ids is the wake-printed prefix. alpha/beta presence is the leaf contract, not exact dated lines. | unit | — |
| test_zoom.py | 123 | test_ambiguous_prefix_is_error | zoom raises ValueError when a prefix matches two ids. SUT entry points: named_ids monkeypatched to two colliding ids; zoom_text(repo, 'a3f2c1b8'). Assertion targets: pytest.raises(ValueError, match='ambiguous'); message contains Give a longer prefix. Fixture shape: two notes present but ids replaced by the patch. The two synthetic ids share a3f2c1b8; named_ids is patched so resolve_id sees only those identities. match=ambiguous is the pytest.raises primary oracle. | unit | semantic-redundancy? |
| test_zoom.py | 136 | test_zoom_skips_unreadable_sibling_warns | Zoom of a nested id still works and warns when a sibling children file is unreadable. SUT entry points: two naps; corrupt first.tree_path; zoom_text(child_id). Assertion targets: child text in stdout; stderr exactly skipped a pack\n; no notes/, naps/, git, or Traceback. Fixture shape: A/B and C/D packed; child_id from second tree's first note digest. named_ids/zoom walk every view nap; a sibling {not json must warn and continue so the nested leaf in the good pack still prints. | unit | semantic-redundancy? |
| test_zoom.py | 164 | test_named_ids_skips_non_mapping_tree_child | A children file whose child is not a mapping does not make named_ids raise. SUT entry point: named_ids(repo) after writing {"c":[1]} to the nap tree. Assertion target: nap.id is in the returned list (no exception). Does not assert the bad child was omitted. Fixture shape: two-note nap with a non-mapping c entry. c:[1] is valid JSON but not a child object; named_ids must keep the view nap id and not raise. Nested collect is skipped for that tree. | unit | — |
| test_zoom.py | 176 | test_zoom_non_mapping_child_is_unreadable_pack | zoom_text on a nap whose tree child is not a mapping raises unreadable pack. SUT entry point: zoom_text(repo, nap.id) after {"c":[1]} tree bytes. Assertion targets: ValueError match unreadable pack; message omits notes/, naps/, git; stderr empty after the raise. Fixture shape: two-note nap; capsys drained first. Zooming the nap itself must fail closed when kids are not mappings; skipping is only for sibling trees while targeting another id. | unit | semantic-redundancy? |
| test_zoom.py | 194 | test_zoom_skips_sibling_non_mapping_child_warns | Zoom of a nested id still works and warns when a sibling tree child is not a mapping. SUT entry points: two naps; first tree set to {"c":[1]}; zoom_text(child_id). Assertion targets: child text in out; stderr exactly skipped a pack\n; no paths, git, or Traceback. Fixture shape: same four-note two-nap layout as the unreadable-sibling test. Same skip+warn contract as a JSON-corrupt sibling, but the sibling tree is well-formed JSON with a non-object child. | unit | semantic-redundancy? |
| test_zoom.py | 222 | test_zoom_unreadable_tree_is_unreadable_pack | zoom_text on a nap whose .tree is not JSON raises unreadable pack. SUT entry point: zoom_text after {not json bytes. Assertion targets: ValueError match unreadable pack; no store paths or git in the message; stderr empty. Fixture shape: two-note nap; capsys drained. Targeting the broken nap (not a nested id) is a hard error, not a skip. Empty stderr means the warning path was not used. | unit | semantic-redundancy? |
| test_zoom.py | 240 | test_zoom_nested_note_id_prints_dated_leaf | Zoom of a note id that lives only inside a children file prints a dated leaf. SUT entry points: write_nap; loads_tree; leafset_id of first child's file bytes; zoom_text(cid). Assertion target: exact dated_leaf(stamp from child.name, child.text) plus newline. Fixture shape: two-note nap; nested id not a view node. The nested id is leafset_id of the child's file bytes, which is what wake would print after expand. Stamp is the first hyphen field of child.name. | unit | — |
