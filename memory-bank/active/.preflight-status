PASS WITH ADVISORY

Findings:
- **TDD Plan Encoding**: PASS. All executable units have test steps scheduled before production code.
- **Convention Compliance**: PASS. Proposed file locations and naming conventions align with system patterns.
- **Dependency Impact**: PASS. Backward compatibility with `surgery.py` is explicitly preserved.
- **Conflict Detection**: PASS. No conflicts with existing patterns or sibling PRs.
- **Completeness Precheck**: PASS. All requirements are addressed.

Advisory items:
- **Radical Innovation**: Consider encapsulating the view state (nodes and config) into a lightweight `StoreContext` object passed through the write path, rather than adding multiple optional kwargs (`nodes=`, `entry_chars=`) to `write_nap` and `fold_request`. This would simplify signatures and make future threading easier.
