PASS WITH ADVISORY

Task: docs-sunset (Level 2). Run: Preflight, 2026-08-19.

TDD plan encoding: PASS. All six implementable units are prose/policy (VISION/ROADMAP triage, `docs/architecture/index.md`, `docs/notes.md` + `docs/index.md`, README, memory-bank reconciliation, deletion of the two files), so none owe tests. Verified the no-executable-behavior claim independently rather than accepting the plan's `*.py` survey: `git grep -E 'VISION|ROADMAP'` over the tracked tree, excluding `memory-bank/archive/` and the two files themselves, hits only memory-bank prose. The driver `summem` has no `.py` suffix and is clean; `tests/`, `pytest.ini`, `AGENTS.md`, and `.cursor/` are clean. Deleting both files changes nothing that executes. No change-detector steps to strike; the plan explicitly forbids README/`docs/` content assertions. No test/production step reordering needed.

Convention compliance: no prior `docs/` convention exists in this repo (no `docs/` directory, no mkdocs config), so the stockroom-shaped `docs/index.md` + `docs/architecture/index.md` + `docs/notes.md` layout introduces one rather than conflicting with one. That layout is consistent with the sibling repo the brief points at, and step 3 stays inside constraint 1 (markdown in mkdocs shape, no site).

Broken skill citation (minor, fixable in build): step 2.1 cites `.cursor/skills/ai-rizz/architecture-docs/SKILL.md`. This repo has no `.cursor/skills/ai-rizz/`; only `.cursor/skills/shared/`. The architecture-docs skill lives at the user level, `~/.cursor/skills/ai-rizz/architecture-docs/SKILL.md`, and is in the agent's skill list either way. The co-cited `.cursor/skills/shared/illustrate-complexity/SKILL.md` is correct. Build should read the user-level path; no plan restructuring needed.

Dependency impact: the living citation surface is exactly three persistent memory-bank files. `productContext.md` cites VISION twice (line 3 design contract, line 32 "First proof" as the success bar) - both are inside step 5.1's scope. `systemPatterns.md` cites it once (line 13) - step 5.2. `techContext.md` cites it four times: line 3 "The first backend specified in `VISION.md`", line 3 "the CLI table in `VISION.md` must not", line 21 "proofs ... listed in `VISION.md` under 'First proof'", and line 25 in Canonical documents. Step 5.3 names only the last two.

Dangling-citation gap (minor, fixable in build): after step 6 deletes the files, `techContext.md` line 3's opening clause and line 21's Testing Process sentence would point at a nonexistent file unless build fixes them. Step 6.2 is the catch-net, but it is worded as a grep for "living 'VISION.md is the contract' citations", and neither of those two reads as a contract citation. Build should execute 6.2 as "any citation of either deleted file outside `memory-bank/archive/` and this task's ephemeral files", and redirect line 21's acceptance-bar sentence to `tests/test_proof_*.py` the same way step 5.1 redirects `productContext.md` line 32. That is a reading instruction, not a plan change, so this does not gate build - but it is the single most likely way this task ships a wrong citation, which constraint 3 forbids.

Conflict detection: no duplication-in-waiting of substance. `README.md` is tracked and empty, so step 4 fills a placeholder rather than competing with existing content, and ROADMAP's own Later list already names "A filled README.md" as outstanding. Nothing in the tree already provides an architecture page. No public contract moves: the agent CLI (`wake`, `note`, `nap`, `recall`, `zoom`, `start`, `init`) and the `AGENTS.md` activation block are untouched. Watch item, not a conflict: the CLI surface will then be described in three places - `usage_text` in `summem`, `AGENTS.md`, and the new README table. That is inherent to documenting a public interface, and step 4.3 already keeps the README pointing at `AGENTS.md` instead of duplicating the baked block; keep the README table short so the drift surface stays small. Second watch item: `systemPatterns.md` (briefing) and `docs/architecture/index.md` (atlas) will overlap by construction. Step 5.2 resolves it by keeping briefing altitude and pointing at the atlas; QA should check that direction of reference rather than treating overlap itself as a defect.

Completeness precheck: all seven requirements have a concrete step with named files - req 1-2 to step 1, req 3 to step 3.1, req 4 to step 4, req 5 to step 5, req 6 to step 3, req 7 to step 2. Acceptance criterion 1 maps to step 6.1. Constraint 1 (no mkdocs site) is respected: no `mkdocs.yml` or `.pages` appears anywhere in the plan. No requirement is acknowledged without an implementation path. Test coverage: none owed, per the TDD check above.

Triage buckets spot-checked against `summem`, since a wrong bucket would misdirect build. Confirmed correct: hot margin and pack-size cap are absent from the script (only `ENTRY_CHARS = 280` and `WAKE_LINES = 32` exist, lines 153-154), so both are genuine leftovers; the baked prompt says "Otherwise run `.summem/summem wake` from the repository root" with no "do what it prints", so VISION's wake-as-a-script-to-do framing is built-else. One bucket item is imprecise: "any remaining 8-character-id picture vs unique prefix of 64 hex" reads as built-else, but VISION already says "shortest unique hex ... at least 8 characters" and `short_id`/`unique_prefix` in `summem` implement exactly that, so it is true-of-tree, not built-else. The plan already labels the list "confirm in build; do not treat this list as closed", so this is a note for build's confirmation pass rather than a plan defect.

Radical innovation (advisory, do not implement without operator direction): anchor the architecture page's invariants to the proofs that defend them. The real risk of retiring VISION is not losing prose - it is that VISION's invariant list was the only place those guarantees were named, and a fresh architecture page inherits that same "trust the document" weakness. Concretely: give each invariant on `docs/architecture/index.md` a short stable name (ingest-commutes, sequence-in-filename, tree-write-once, sum-is-the-only-honest-conflict, zoom-is-a-property-of-HEAD, wake-is-wait-free, no-shared-mutable-index, empty-packages-stay-empty, root-pushes-children-pull, knobs-live-in-the-store), and in each existing `tests/test_proof_*.py` module docstring name the invariant(s) that module defends. Then the architecture page's invariant section is a table of invariant name, one-sentence statement, and the proof module that would go red - and `grep` over `tests/test_proof_*.py` answers "is this invariant actually enforced?" without adding a single test. Invariants with no proof module become visible as such, which is information the sunset would otherwise destroy. This adds naming and docstrings only; it writes no new assertions and no change-detector, so it stays inside this task's no-new-tests posture. Cost is real but bounded (ten names plus eight docstrings), and it is the difference between an architecture page that is a document and one that is checkable.

Verdict: the plan is buildable as written. Two minor findings (broken skill path in step 2.1, under-scoped grep in step 6.2 leaving two dangling `techContext.md` citations) are absorbable during build without re-planning, one bucket label needs correcting during step 1's confirmation, and the invariant-anchoring idea is left to the operator.
