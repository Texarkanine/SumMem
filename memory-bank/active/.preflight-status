PASS WITH ADVISORY

TDD Plan Encoding: PASS. Units 1–3 (locate; break-out/unlink/heal; CLI) each number stub-tests → stub-interface → red tests → green code. Unit 4 is prose/policy with no tests. No change-detector steps. No test/production order swap.

Convention: PASS. Repo-root `surgery.py` (not a summem subcommand) matches issue #28 and the brief. Tests live in `tests/test_surgery.py`. Named helpers exist on repo-root `summem` with the signatures the plan calls: `list_view`, `rematerialize_child`, `_note_children`, `loads_tree`, `heal_view`, `_unlink_node`, `with_store_lock(parent, fn)`, `resolve_parent`, `require_python`. `rematerialize_child` already copies existing `NapChild.sum` and will not clobber dest — the plan's "copy captions / never write_nap" is that function. `_payload_names` is a local test helper in `tests/test_zipper.py` (also `test_nap.py`, `test_wake_expand.py`); copy it into `test_surgery.py`. Plant overlapping packs with `_plant_nap` / `rematerialize_child` as zipper tests do (`write_nap` cannot leave two overlapping view naps).

Dependency: PASS. No edits to `summem`, `prompt_text()`, `docs/agents-prompt.md`, or `AGENTS.md`. Existing suites need not change. `tox.ini` `--cov=summem` stays; surgery.py is out of default coverage as planned. `heal_view` subset-drops a rematerialized loose note still covered by a larger overlapping pack (`test_heal_note_covered_by_nap_dropped`) — the plan's "no heal_view inside the break-out loop" is required.

Conflict: PASS. No existing `surgery.py`. No delete/forget CLI. Public command table and activation block stay untouched. Underscored helpers are already called from tests; the issue names `_unlink_node`.

Completeness: PASS. Requirements 1–9 and acceptance criteria map to units 1–4 with files and functions named. `--dry-run` / `--contains` / filename addressing are in the test list. Operator docs cover tip-then-history-rewrite and agent aftercare without adding surgery to the command table.

Advisories (do not change the plan):
1. `with_store_lock(parent, fn)` is a callback wrapper, not a context manager. Copy `summem.main`'s `note_locked` pattern. `with m.with_store_lock(parent)` will fail.
2. `_unlink_node` takes a `ViewNode`. After rematerialize, resolve the nap/note from `list_view` by stem/filename.
3. When two view naps contain the target, pick them in `list_view` filename order so the dry-run chain is deterministic.
4. Inject conftest `load_summem()` into `locate_note`/`excise_note` so the `write_nap` monkeypatch observes the same module object. `surgery.main`'s own SourceFileLoader is a second copy — do not rely on patching it in unit 2. Load `surgery.py` from tests via SourceFileLoader (or a repo-root import); pytest.ini has no `pythonpath`.
5. Lock, `require_python`, and driver-load are listed as behaviors but have no named stubs; they are in the CLI write-code step and already tested on the helpers. Fine as-is.
6. If both `--contains` and the positional name are present, pick one rule (name wins, or error). `test_main_usage_without_target` only locks the neither-address case (exit 2).

Radical innovation (advisory, not applied): after a mutating excise and `heal_view`, if `fold_request(parent)` is non-empty, print that prompt on stdout after the chain. Same bytes `note`/`nap` already emit. Makes aftercare "run an agent" mechanical without inventing captions or calling `write_nap`.
