PASS WITH ADVISORY

Prerequisites pass. This is a completed Level 2 plan, and no creative phase was flagged or required.

TDD plan encoding passes. Each executable unit schedules empty test stubs and interface stubs before implemented tests, a focused red run before production behavior, and green plus broader regression runs afterward. The documentation unit is correctly identified as prose and has no change-detector test.

Convention compliance passes. The plan changes the repo-root `summem`, extends the existing pytest modules, and updates the canonical architecture atlas. The proposed `_recall_nested` helper avoids changing `_note_children`, whose existing note-only contract is required by `_nap_stem` and rematerialization.

Dependency impact passes. The plan accounts for `NapChild`, `NoteChild`, `loads_tree`, `_TREE_PARSE_ERRORS`, view formatting, CLI stderr propagation, the existing malformed-tree recall test, the existing nested-zoom sibling test, direct zoom failure behavior, wake silence, and the full regression suite.

Conflict detection passes. No existing warning helper or nested-caption recall walker overlaps the proposed additions. The warning is placed only on parse-error `continue` paths, preserving the public `unreadable pack` failure for an explicitly requested view nap.

Completeness passes. Concrete steps cover nested-caption matching and line shape, path-safe output, retained original-note recall, warning text and success semantics for both zoom and recall, the lone unreadable recall case, asked-for zoom failure, wake silence, and architecture documentation.

Advisory: consider replacing the several purpose-specific recursive tree walkers with one typed depth-first traversal in a future redesign. A concrete shape would be `_walk_tree(tree)` yielding note and nap records with canonical ids, captions/text, and child trees; `_collect_ids`, `_find_in_tree`, and recall formatting could consume that stream while file-read error policy remains at each command boundary. This would make new searchable tree payloads accretive in one place, but it is broader than this task and the current focused plan should not be changed for it.
