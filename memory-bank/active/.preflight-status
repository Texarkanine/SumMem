FAIL (fixable)

TDD Plan Encoding: both executable units (length ratchet, other agent-facing ratchets) number stub-tests → write-tests-red → write-code-green. The atlas unit is prose/policy and correctly schedules no tests. No change-detector strike. No step-order swap.

Convention: files, pytest locations, `require_entry(text, entry_chars=None)`, and CLI-via-`main` match `systemPatterns.md` / `techContext.md`. No new module, no new public function. Existing leak and proof-5 substring checks stay valid if distinctive phrases are kept.

Dependency: CLI already prints `ValueError` via `sys.stderr.write(f"{exc}\n")` after an early `require_entry` and again inside `write_note` / `write_nap`. Changing `require_entry` covers both note and nap, library and CLI. `test_config_entry_chars_is_per_store_for_notes_and_naps` already drives a tight store at `ENTRY_CHARS = 5`. Exact-match stderr tests (`skipped a pack\n`) sit on rows the table leaves unchanged.

FAIL (fixable) — shared `require_entry` multi-line copy is note-only. Unit 2 schedules `note must be one line` → `One line only. Merge the lines, or note each line.` That function also validates nap captions. “Note each line” is false for `summem nap`. Known fix: a next step that is true for both paths (e.g. `One line only. Merge the lines.`), or a kind-specific message. Do not ship the current row.

FAIL (fixable) — the closed table keys `unknown id` as one row, but that exact string is raised for two causes. `resolve_id` and `_adjacent_nodes` mean the token is not in the view; “Copy an id from wake.” is a known next step. `_as_child` (line 504) and `zoom_text` (line 609) raise the same string after the id was already resolved, when a view nap has no readable `.tree`. That is the same class as `unreadable pack`: no known next step that is not invented. A global string replace would attach the wake hint to the missing-tree path. Known fix: attach the next step only at the identity-miss sites; leave the missing-tree raises as problem-only `unknown id` (keep the substring).

Advisory — `note is empty` already states the problem; the next step is obvious. Rewriting it to `Empty.` is style, not a ratchet gap, and Unit 2’s stub list does not schedule a wording assert. Existing `test_note_rejects_empty` / nap empty only check `ValueError`. Prefer leave the string, or add a fact assert if the rewrite stays.

Advisory — Behaviors to Verify includes 94 × `你` → footer names `282` bytes. Unit 1’s named stubs are ASCII overlong plus the tight-store CLI case. `len(text)` in the footer would still pass those. Put the UTF-8 overflow number in a written test (new case or inside `test_note_overlong_message_is_a_ratchet`).

Advisory — `tests/test_cli.py::test_note_error_text_omits_store_paths_and_git` is already the CLI over-long note path (281 bytes, exit 1, leak checks). Extend it with footer asserts instead of adding a sibling `test_cli_note_overlong_prints_ratchet`. There is no existing CLI nap-overlong test; that new case is warranted.

Advisory — extending `test_config_entry_chars_is_per_store_for_notes_and_naps` with stderr asserts requires `capsys` and a drain after each failing `main` (`toolong` note and `toolong` nap). A single read at the end mixes both failures.

Advisory (radical) — replace Unit 2’s unique-string table with a raise-site table: one row per `raise ValueError` / `stderr.write` in `summem` (~15 sites), each with cause and next-step-or-none. Shared phrases stop deciding the next step; the two FAIL rows become structural instead of copy nits. Do not apply that redesign in this preflight.
