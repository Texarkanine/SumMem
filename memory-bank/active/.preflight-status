PASS WITH ADVISORY

Prerequisites: Level 2 plan is complete (initialization, TDD planning, implementation plan, technology validation, pre-mortem). No creative-phase docs are required or present.

TDD plan encoding: pass. Unit 1 (dated leaf printer) is stub tests → stub `_day_from_stamp` → write tests red → write code green. A reasonable implementer cannot follow those numbered steps by coding first. Unit 2 is prose/policy (briefing + `prompt_text` lockstep); omitting tests for those artifacts is correct. No change-detector steps to strike. No test/production order swap.

Convention: pass. The helper and printer stay in repo-root `summem`; tests stay in existing `tests/test_wake.py`, `tests/test_wake_expand.py`, and `tests/test_fold.py`. `_day_from_stamp` matches the historical private-helper name. Grain-1 `ProjectedNode(kind="nap", leaves=1)` is the right fixture: public `nap` never produces grain 1.

Dependency: advisory. `format_wake_line` feeds `wake_text`, `fold_request`, and `recall_text`'s view pass. Wake/expand/fold retargets are in the plan. Recall already asserts substrings of the stored sentence (`tests/test_recall.py`), so those stay green when the view line gains a prefix — matching the listed recall edge. `tests/test_nap.py` exact-matches a pack line (`x2 {prefix}: pair`) and stays green if packs stay undated.

Unlisted consumer: `tests/test_proof_ingest.py` asserts `set(lines[1:-1]) == {"alpha", "beta"}` on CLI `wake` after `note` (writer time is "now" UTC). That exact set goes red. The Challenges grep (`== "hello"`, `  alpha\\n`, and kin) does not find it. Full `tox` in unit 1 step 4 will. When retargeting, derive the day from each note filename stamp (or `datetime.now(timezone.utc).date()`), not `date.today()`.

Conflict: pass. `_day_from_stamp` is not in `summem` now (tree-schema dropped it from the printer; `store_stats` is gone). No duplication. Zoom stays `{id}  {text}` (`_zoom_note_line`, `_recall_nested` untouched). `resolve_id` already rejects a hyphenated day; `is_range_token("2026-08-24")` is false (`partition` leaves `08-24` on the right). The wake print contract change is the task; unit 2 updates `systemPatterns.md`, the architecture invariant, and the `prompt_text` / `docs/agents-prompt.md` / `AGENTS.md` lockstep (`tests/test_init.py`).

Completeness: advisory, not blocking. All seven brief requirements have a file-and-function path. Empty-caption trailing-colon is specified in unit 1 step 4 but has no red test in step 3; `require_entry` rejects `write_note("")`, so that case is printer-only (constructed node or an empty on-disk note). Nested `NoteChild` expand is listed as an edge and is not a dedicated step-3 case; dating `kind == "note"` plus `_projected_child`'s `stamp=child.name.split("-")[0]` covers it. Missing/conflict `.sum` tests already exact-match undated `x2 {prefix}:` lines.

Radical innovation (advisory, not applied): add a tiny test helper, e.g. `dated_leaf(stamp, text)` in `tests/conftest.py` that slices the 16-char stamp the same way `_day_from_stamp` will, and use it for every exact leaf expectation — including `test_proof_ingest.py` reading the written note names. One construction, no `date.today()`, and the Challenges grep sweep becomes "replace callers of the helper" instead of hunting leftover bare strings.
