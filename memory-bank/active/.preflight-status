PASS WITH ADVISORY

Preflight of nap-ack (Level 2), 2026-08-28. No plan edits were made; no change-detector steps were scheduled and no TDD ordering swap was needed.

TDD plan encoding — PASS. Unit 1 ("Nap CLI stdout — executable") orders its numbered substeps stub tests, stub interface, write tests and run red, then write code and run green; an implementer cannot follow it by coding first. Unit 2 ("Operator and briefing copy") delivers README and `systemPatterns.md` prose, owes no tests, and the plan says so. No numbered step asserts on a document's contents, so nothing to strike. The Pre-Mortem explicitly declines to phrase-lock `how_to_text` / `prompt_text`, which is the right call.

Convention compliance — PASS. `tests/test_fold.py` and `tests/test_cli.py` are the files that already own this behavior; `test_<behavior>` naming, `tmp_path` + `init_repo`, `monkeypatch.chdir`, `capsys`, and the `summem` session fixture all match the suite. The iteration command `tox -e py311 -- <ids>` matches `techContext.md` and the repo testing rule. Editing repo-root `summem` alone is correct: `.summem/summem` and `dogfood/.summem/summem` are symlinks to it (verified with `ls -l`).

Dependency impact — PASS. Every other CLI `nap` call site in the suite either discards stdout with a bare `capsys.readouterr()` (`tests/test_cli.py` lines 62, 317, 337, 392; `tests/test_zipper.py` line 744; `tests/test_scopes.py` lines 152, 373) or asserts on stderr only. The two cases in `tests/test_fold.py` that the plan retargets (lines 235-263) are the only tests that assert nap stdout content. `tests/test_scopes.py` `Saved.` assertions are all on the `note` arm and are unaffected. `fold_request` itself is untouched, so `surgery.py` line 246 and the direct-call tests (`tests/test_zipper.py` line 649, `tests/test_fold.py` lines 322-419) stay green.

Conflict detection — PASS. The idle string is verified verbatim against OptMem (`~/.local/bin/memo` lines 770 and 785 print `Nothing left to compress.`), and OptMem's remaining-count copy at lines 523-524 matches the one `fold_request` already builds. Nothing in the codebase already prints a nap ACK, so there is no duplication-in-waiting beyond the four-line spacing block noted below. The plan changes stdout only; no public contract in the README command table moves.

Completeness precheck — PASS. Brief requirements 1-5 each map to concrete steps: 1 and 2 to step 1.4's if/else on `out` plus the two retargeted tests, 3 to the untouched `note` arm guarded by `test_over_budget_note_requests_equal_grain_ones`, 4 to the new `test_rejected_nap_does_not_print_saved` alongside the existing `test_cli_nap_overlong_prints_ratchet`, 5 to step 1.1's explicit keep-and-rename. The behavior "fold_request() still returns empty at budget" needs no new test: `tests/test_zipper.py::test_heal_to_8_2_1_empty_fold_request_wake_projects` already pins it. Step 2.1's README claim checks out — that example's post-nap view is four nodes under the default budget of 32, so `fold_request` returns empty and the new bytes there are `Saved.` then `Nothing left to compress.`

Advisory 1 — assert the blank line, not just the prefix. The behavior list says mid-cascade stdout is `Saved.`, a blank line, then the pair. Step 1.3 only schedules `out.startswith("Saved.\n")`, which passes even if the separator is dropped; only the at-budget case pins exact bytes. Cheap fix: assert `out.startswith("Saved.\n\n")` in the mid-cascade cases.

Advisory 2 — the step 1.2 comment. "Success stdout is ACK then fold or idle" narrates the three lines directly under it and duplicates the `systemPatterns.md` sentence. If a comment goes on that arm, make it the non-obvious constraint instead: the idle line is nap-only and must not move into `fold_request`, because `note` shares that helper. Otherwise skip it.

Advisory 3 — `docs/surgery.md` Aftercare. Lines 47 and 54 tell the surgeon to "repeat until there is no fold request," which is the same silence this task removes, one layer up. Not falsified by the change (`surgery.py` still prints only a fold request), so not a plan defect, but the operator recipe will read as stale next to a `nap` that now announces the end of the cascade.

Advisory 4 (radical innovation, do not implement) — promote the write-result print to a named CLI-layer contract, then give it to `surgery.py`. Concretely: module constants `ACK = "Saved.\n"` and `IDLE = "Nothing left to compress.\n"`, plus one helper `emit_result(body: str, *, ack: bool, idle: bool) -> None` that writes the ACK, a blank line, and either `body` or the idle line. `note` calls it with `ack=True, idle=False`, `nap` with `ack=True, idle=True`, and `surgery.py`'s post-excision print (line 246) with `ack=False, idle=True` — which is what closes the Advisory 3 gap for real rather than by documentation. The cost is one helper and three call-site changes against four inline lines duplicated twice; the payoff is that "what a writer prints when the cascade is over" becomes one testable function instead of a phrase repeated across two files and a doc. This is out of scope for a Level 2 and should not be bolted on now; it is the shape to reach for if a third writer surface ever needs the same signal.
