PASS WITH ADVISORY

TDD Plan Encoding: PASS. Units 1–3 are stub-tests → stub-interface (or none) → write-tests → write-code. Unit 4 is prose/policy and schedules no tests. Token pins on how_to_text()/prompt_text() are the existing product-output contract style, not document change-detectors. Lockstep tests are dual-file contracts. No step swap or strike.

Convention: PASS. how_to_text() beside catalog_text()/prompt_text()/usage_text(); composition stays in the existing wake branch; usage_text remains the operator -h catalog (CLI_NAME); AGENT_BIN stays on agent-facing text. Test files are the ones already used for prompt and root-wake shape.

Conflicts: none that break the public contract. No existing how_to_text(). Pull omit of catalog is already the pattern; Usage-omit is the same gate (parent is root). Plan still puts the Usage header inside how_to_text() (catalog_text() shape).

Completeness vs brief: the four requirements and the pull/init/lockstep/0BSD constraints have concrete files and steps.

Prior FAIL (fixable) leftover pins are now named in numbered steps:
1. Unit 1 pins clone / another machine (and a bare git forbid) on how_to_text(); unit 2 drops those tokens from test_prompt_text_invariants.
2. Unit 3 slices the ingest proof as set(lines[header+1:-1]) from the memories header to the footer, and does not require that header at lines[0].
3. README-only one-time shrink; init_text() stays the new-install wrapper. test_cli.py is off the unit 3 file list.

Advisory (not blocking):

- test_proof_ingest.py still has assert lines[0] == "== Project-root Memories ==". Integration Tests and unit 3 say find the header and do not require lines[0]; a step-only reader could keep the identity assert. Drop it when retargeting.
- test_ignored_store_omitted_from_catalog still forbids "wake --path pkg" / "wake --path secret" on the whole root-wake stdout. how_to_text() must teach wake --path <path>, not pkg as the example path.
- test_root_wake_catalogs_other_store still forbids "notes/", "naps/", and "git" on the whole stdout. Unit 1 already forbids those in how_to_text(); do not put them back.
- Bootstrap will name == SumMem Usage == as the skip key, so that string lives in always-injected AGENTS.md. The predicate must stay "see and follow a Usage block", not "see the heading substring", or a grepping agent never wakes.
- how_to_text() must not mention == Project-root Memories == or == Additional SumMem Catalogs ==, or header-find tests (and skip-block identity) break.
- WAKE_LINES / fold pressure has no new test. Plan already says existing fold tests call wake_text()/fold_request() and never see Usage. Low risk if composition stays in the wake branch.

Radical innovation (advisory, not applied): assemble the root-wake document from named optional sections instead of inlined prepends and full-stdout exact strings. Add wake_document(root, parent) that joins how_to (root only), catalog, Project-root header + wake_text (root and non-empty), and the footer, with the same blank line between non-empty sections. Tests ask for sections(out)["usage"], sections(out)["catalog"], sections(out)["memories"] rather than out == "…" or lines[0]. That is the leftover-pin class that already failed the first preflight. Cost is a small joiner and retargeting the two exact-string cases in test_scopes.py. Version-keyed Usage (prior radical) remains declined.
