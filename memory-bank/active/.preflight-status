FAIL (fixable)

1. **TDD Plan Encoding (fixable) — step 4's malformed-JSON tests are green before the change.** Verified against the working tree: `json.JSONDecodeError` is a `ValueError` subclass, so today `zoom_text(repo, nap_id)` on a `{not json` `.tree` already raises `ValueError`, and `main(["zoom", nap_id])` already returns rc 1 with stderr `Expecting property name enclosed in double quotes: line 1 column 2 (char 1)` — no traceback, no `notes/`, `naps/`, or `git`. As written, `test_zoom_unreadable_tree_is_value_error` and `test_cli_zoom_malformed_tree_returns_1` pass before step 4's production change, so nothing forces the malformed-JSON site to be wrapped or the leaked parser message to be replaced. Only the `OSError` case is genuinely red. Fix: have both malformed-JSON assertions require the designed message (`ValueError` matching `unreadable pack`; CLI stderr containing `unreadable pack`), the same way the existing `test_cli_malformed_tree_returns_1_without_traceback` in `tests/test_cli.py` already asserts it for the nap path.

2. **Completeness (fixable) — step 4's second `loads_tree` site has no planned test.** Step 4 wraps both `loads_tree` calls in `zoom_text`: the direct nap lookup and the nested `_find_in_tree` search. The planned tests only reach the direct lookup. The nested site is reachable: zoom a valid nested id while a *different* nap in the same view has an unreadable `.tree`, since `named_ids` swallows that nap's parse failure but the search loop still opens it. Either add that case or drop the second wrap from the plan so no untested production change ships.

3. **Resolved prior findings.** The catalog prefix now has a real red (`test_catalog_omits_store_driver_path`; `usage_text()` still emits `.summem/summem` today, and the existing `"summem wake" in catalog` assertion cannot drive the change). `CLI_NAME` centralizes the printed name, and defining it beside `_COMMANDS` is safe even though `catalog_text` and `fold_request` are defined earlier. Step 3's recall red is confirmed empirically: with `WAKE_LINES = 4` and 11 loose notes, `recall_text(repo, "n0")` returns `''` today. Steps 5, 6, and 7 match the code: `fold_request` hardcodes 280 at the prompt line, `test_write_nap_note_inside_adjacent_nap_raises` still has the loose `or any(...)` assert, `test_unreadable_tree_does_not_split` still uses `chmod`, `test_cli_nap_overlapping_ids_exits_0_without_concat` is in `tests/test_zipper.py`, and the `present` guard after `resolve_id` is dead because `resolve_id` only ever returns a member of `view_ids`. Step 9 and 10 cover every remaining `.summem/summem` string in `VISION.md`, `systemPatterns.md`, and `techContext.md`.

4. **Advisory — recall drops nested nap captions.** Today `recall_text` searches `wake_text` output, which for an under-budget store includes in-memory expansion, so a mid-tree nap caption can match. The planned implementation searches top-level `format_wake_line` rows plus `_note_children` leaves, and `_note_children` yields only `NoteChild`, so intermediate captions inside a `.tree` stop being searchable. The accepted review item only asks for "view captions plus `.tree` originals", so this is within the brief — but if the loss is unintended, also matching `NapChild.sum` while walking the tree is a one-line addition.

5. **Advisory — CLI-boundary test placement.** `tests/test_zoom.py` is currently in-process API tests only; every existing CLI-arm test (including the malformed-tree rc-1 case) lives in `tests/test_cli.py`. Step 4 puts two new `main(...)` tests in `tests/test_zoom.py`. Either location works, but `tests/test_cli.py` matches the existing convention.

6. **Advisory — step 1's rationale is inaccurate, though the test is still red.** `.summem/summem` exists in this working tree as untracked local store data, so the suite passes locally and would only fail on a clean checkout; collection does not fail today because `load_summem()` runs inside each test. The red comes from the `SCRIPT == ROOT / "summem"` half, not from `SCRIPT.is_file()`.

7. **Advisory — knowingly stale copies.** `ROADMAP.md` lines 34 and 44 will still say the script lives at `.summem/summem`, and the tracked `dogfood/.summem/summem` keeps the old help text. Step 10 deliberately excludes ROADMAP and dogfood; recording it so the contradiction is a decision, not a surprise at QA.

8. **Advisory — radical innovation.** One agent-facing error seam. Add `class AgentError(ValueError)` plus `def fail(msg)` to the driver, route every agent-visible failure through it (`unknown id`, `ambiguous id`, `overlapping packs`, `unreadable pack`, entry-length rejections, `not in a repository`), and have `main` catch `AgentError` for rc 1. Then replace the vocabulary assertions currently duplicated across roughly eight tests with one parametrized sweep over the bad-input matrix (malformed tree, unreadable tree, unknown id, ambiguous prefix, over-long note, no repository) asserting the shared invariant: no `notes/`, no `naps/`, no `git`, no `Traceback`, and no parser internals. The leaked `Expecting property name enclosed in double quotes...` message this run verified is exactly the failure such a seam makes structurally impossible instead of test-by-test. Cost is roughly ten raise sites plus one CLI catch; do not do it in this task — weigh it against the punch-list scope.
