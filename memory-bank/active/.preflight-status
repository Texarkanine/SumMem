PASS WITH ADVISORY

TDD Plan Encoding: both executable units number stub-tests → write-tests-red → write-code-green. The atlas unit is prose/policy and correctly schedules no tests and no change-detector. No step-order swap.

Convention: files, pytest locations, `require_entry(text, entry_chars=None)`, and CLI-via-`main` match `systemPatterns.md` / `techContext.md`. No new module, no new public function.

Dependency: CLI already prints `ValueError` via `sys.stderr.write(f"{exc}\n")` after an early `require_entry` and again inside `write_note` / `write_nap`. Changing `require_entry` covers note and nap, library and CLI. Existing substring checks (`unknown id`, `ambiguous`, `16-31`, `unreadable pack`) stay valid if distinctive phrases are kept. Exact-match stderr tests (`skipped a pack\n`) sit on rows the table leaves unchanged. `pytest.raises(..., match="ambiguous")` still matches the longer line.

Conflict: no existing ratchet helper to reuse. Public CLI strings are the change, not a silent contract break. Proof 5 keeps `unknown id` and the range token.

Completeness: Unit 1 maps the must-ship length footer (bytes, configured limit, accented hint, compress) onto `require_entry` plus the existing CLI note leak test and a new CLI nap case. Unit 2’s raise-site table covers every agent-facing `ValueError` / `stderr.write` in `summem`. The prior FAIL (fixable) items are fixed: multi-line next step is merge-only (true for nap); wake hint is only on `resolve_id`, `_adjacent_nodes` no-hits, and `zoom_text`’s final raise; `_as_child` and `zoom_text` missing-`.tree` stay problem-only `unknown id`. Missing-tree tests (unlink a view nap’s `.tree`, then `zoom_text` / `write_nap`) are implementable against lines 609 and 504.

Advisory — tight-store behavior text says 6 bytes, but `test_config_entry_chars_is_per_store_for_notes_and_naps` already sends `toolong` (7 bytes) at `ENTRY_CHARS = 5`. When extending that test, assert the UTF-8 length of the string you send, or change one call to a 6-byte line. Do not assert `6` against `toolong`. The test has no `capsys` today; add it and drain after each failing `main`.

Advisory — `zoom_text("0"*64)` hits `resolve_id`, not the final raise. After `resolve_id` succeeds, the final raise is a named-but-unzoomable fallback (rare; same files as `named_ids`). Changing that line to include the wake hint is harmless and matches the table. Do not invent a fixture whose only job is to reach line 627 as “identity-miss”; the agent-facing unknown-zoom path is `resolve_id`. The missing-tree case (unlink `.tree`, zoom that nap id) is the one that must not contain `Copy an id from wake.`

Advisory (radical) — encode Unit 2’s raise-site table as one parameterized test: (setup, call, must-contain, must-not-contain). Identity-miss vs missing-tree then cannot drift apart as two independently named cases. Do not apply that redesign in this preflight.
