PASS WITH ADVISORY

TDD Plan Encoding: PASS. All four executable units (canonical LF on read, lock by capability, host invoke + init warning, shebang gate) place stub tests and stub interfaces before "write code" steps, in order. The prior FAIL (fixable) — missing coverage of the `msvcrt` backend itself — is resolved: unit 2 now has `test_with_store_lock_msvcrt_locks_one_byte_and_unlocks` and `test_with_store_lock_msvcrt_retries_then_acquires` against a fake `msvcrt` module, asserting append-mode open, a defined one-byte range before the first `LK_NBLCK`, bounded retry/backoff, and `LK_UNLCK` in `finally`; the `fcntl`-on-file fallback is a separate test from the `msvcrt` backend. Units 5-6 are correctly marked prose/policy with no scheduled tests (no change-detector to strike).

Convention Compliance: PASS. New identifiers (`store_bytes`, `agent_invoke`, `_runtime_lock_path`) follow existing public/private naming; no new files; edits stay inside the one-file driver plus its existing test/doc set.

Dependency Impact: PASS. Verified against the driver: every raw content-addressed read site (`note_digest` callers in `list_view`, `_note_child`, `leaf_digests`; `.tree` reads via `loads_tree` at three call sites; `_nap_caption`) is covered by the plan's canonicalization points, including the `leaf_digests` gap the plan calls out explicitly (it parses `.tree` JSON directly, bypassing `loads_tree`). `surgery.py` and `migrate.py` both call `m.loads_tree`, so they inherit canonicalization without separate edits. `surgery.py:240` and two `with_store_lock` call sites in `summem` (`note`, `nap`) are the only lock consumers; all covered. `AGENT_BIN` usage inside `how_to_text()` and `fold_request`'s `Run:` line is fully enumerated and slated for `agent_invoke()`; `prompt_text()` and `usage_text()` (operator-facing, uses `CLI_NAME`) are correctly left untouched.

Conflict Detection: PASS. No duplicate lock or canonicalization abstraction proposed; `with_store_lock` remains the single primitive. `variant_tag`'s CRLF test targets a defensive identity property (this repo's naps are committed and could be checked out with CRLF) rather than a currently-missing production caller — consistent with the plan's read-surface list, not a conflicting invention.

Completeness: PASS. Every project-brief requirement and acceptance criterion maps to a concrete unit, file, and function. Item 3 (won't-do) and "no Windows support paragraph" correctly require no implementation step (the unchanged state already satisfies them).

Advisory (non-blocking):
1. Unit 2 rewrites `test_cli_wake_on_overlapping_head_writes_nothing` off `fcntl.flock` and onto wrapping `with_store_lock`; once that lands, `tests/test_zipper.py`'s module-level `import fcntl` (line 5) has no remaining reference in that file. Builder should delete that now-dead top-level import rather than leave it stranded.
2. `with_store_lock`'s docstring ("while holding an exclusive flock on the store's naps/ directory") stops being accurate once the fallback lands; fold the update into unit 2's "write code" step.
3. Radical innovation (not applied): collapse the five separate canonicalization call-sites into one read choke-point, e.g. `_read_store_bytes(path) -> bytes` that does `store_bytes(path.read_bytes())`, and have `note_digest`'s raw-byte callers, `loads_tree`, `_nap_caption`, and `leaf_digests` all route through it instead of each calling `store_bytes()` independently. That turns "did this read path canonicalize" from a per-function audit (the exact class of gap the Pre-Mortem flagged) into a single grep-able boundary: any bare `path.read_bytes()` on a notes/naps file outside that helper becomes a visible smell in review.
