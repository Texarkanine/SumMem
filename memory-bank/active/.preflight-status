PASS WITH ADVISORY

Scope of this run: Implementation Plan unit 3, "Membership subject wording — executable" (the post-PR-#74 revision). Units 1 and 2 are shipped and QA-passed; they were not re-preflighted.

TDD Plan Encoding — PASS. Unit 3's numbered steps are stub (no new cases) → write tests and run red → write code and run green, with the red step naming the exact assertions to retarget before any shipped text moves. An implementer cannot follow this order and code first. Unit 2 is a no-op prose/policy unit (no files, no tests) and is correctly exempt. The `work on this repository` pins are product-contract assertions on the shipped agent interface, not change-detectors: `prompt_text()` is the committed `AGENTS.md` bootstrap and `how_to_text()` is root-wake stdout, so the phrase is the product's user-visible behavior, and the same class of pin (`contributor`, `personal`, `do not run it again`) is already the established contract suite for both surfaces. No change-detector strike, no step reorder, no edits made to `tasks.md`.

Convention Compliance — PASS. File targets (`summem`, `tests/test_init.py`, `AGENTS.md`), test location (`tests/test_*.py`), and the iteration command (`tox -e py311 -- tests/test_init.py`) all match `techContext.md`. The plan preserves the `systemPatterns.md` wake-usage split (small committed bootstrap in `prompt_text`, versioned how-to in `how_to_text`) and touches no store, CLI verb, or nap mechanic.

Dependency Impact — PASS. Grepped the whole tree for the membership phrase outside `memory-bank/`: exactly three shipped occurrences — `summem:1185` (`how_to_text`), `summem:1208` (`prompt_text`), and `AGENTS.md:11`. The plan names all three. `init_text()` derives from `prompt_text()` and needs no separate edit; `tests/test_scopes.py:309,320,497` compare root wake stdout to `how_to_text()` by identity, so rewording cannot break them. No second bootstrap copy exists (no dogfood `AGENTS.md`, no README/`docs/` quotation of the phrase).

Conflict Detection — PASS. Nothing in the codebase already provides a shared membership-probe constant, so the plan is not duplicating an existing utility. No public contract breaks: the CLI surface, the README command table, and the store format are untouched. Removing `assert "clone" in lower` from `test_how_to_text_is_the_usage_section` is required, not optional — after the rewording, `how_to_text()` contains no other occurrence of "clone" (verified against the live function body), so retaining that assertion would fail. The plan already prescribes the removal.

Completeness Precheck — PASS. Hand-verified the proposed strings against every surviving assertion in `tests/test_init.py:75-167`. New `prompt_text()` body ("records one short line another contributor needs to work on this repository.") keeps `contributor`, `personal`, `note`, `.summem/summem`, `do not run it again` true and keeps `another machine` / `must still be true after a fresh clone` / `x1 YYYY-MM-DD` absent, while satisfying the new pin. New `how_to_text()` paragraph keeps the genre list, telemetry denylist, and skip-if-nothing clause, keeps `already stored` / `do not retry` / `zoom target` / `wake --path` / `catalog` true and `git` / `notes/` / `naps/` / `Run:` absent. `test_prompt_text_notes_are_part_of_the_work` is untouched by the reworded sentence. Sentence count is unchanged (3 bootstrap, 4 how-to) and the phrase is shorter than the one it replaces, satisfying brief requirement 5 and acceptance criterion 5. All six brief requirements and all five acceptance criteria map onto unit 3 or are already satisfied by shipped unit 1 state.

Low-severity finding (corrected): `tasks.md` Component Analysis had said the how-to test required `another machine`; that was stale after unit 1. It now describes the live repository-work pins and removed portability requirements. The executable steps remain unaffected.

Low-severity finding (informational, no action): the store note `20260828T224532Z-bccf1887bea608d5` records the old probe ("work in this clone") and will be stale after this change. Brief constraint 7 forbids rewriting existing notes, and note `20260829T000716Z-4f13dab49598cbb6` already supersedes it in the view. Nothing to do.

Positive note: the reflection's complaint about the previous plan's imprecise red-step prediction ("red on the new pin and lockstep") is fixed here — unit 3 step 3 predicts red on the two probe assertions only, which is what will actually happen, since lockstep stays green until `prompt_text()` changes.

Radical Innovation (advisory, not applied — third appearance of the same finding): hoist the probe into one module-level constant, e.g. `MEMBERSHIP_PROBE = "another contributor needs to work on this repository"`, and have both `prompt_text()` and `how_to_text()` interpolate it in their f-strings; then change the two test pins from re-typed literals to `assert m.MEMBERSHIP_PROBE.lower() in lower`. This is the second consecutive wording pass that has to hand-edit the same sentence in two places plus the `AGENTS.md` copy, which is direct evidence for the drift class the reflection flagged: a future pass can update one surface and leave the other, and the phrase pins — because each is independently re-typed — would follow the edit rather than catch it. The constant makes two-surface agreement structural instead of manually maintained, converts the pins from change-detector-shaped literals into a genuine cross-surface contract assertion, and costs one constant plus a two-line test change. `AGENTS.md` lockstep is already structurally enforced by `test_agents_md_starts_with_prompt_text`, so this closes the only remaining unguarded seam. Judged, not fixed: the operator should decide whether to fold it into unit 3 (natural fit — same files, same red step) or keep it as a follow-up task.
