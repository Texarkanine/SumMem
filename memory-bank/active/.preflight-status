PASS WITH ADVISORY

Third preflight run on the 16-hex-leafset plan (memory-bank/active/tasks.md), after two prior FAIL (fixable) rounds. Verified against live codebase: summem (leafset_id, _parse_nap_stem, NapChild/Tree, dumps_tree/loads_tree, _digests_of_tree/_digests_of_dict, write_nap, child_nap_stem, nap_stem, variant_tag, _write_pair, list_view, _index_tree, named_ids, short_id, resolve_id), migrate.py (_four_part_stem, _migrate_store, main), tests/test_migrate.py, tests/test_codec.py, tests/test_nap_variants.py, tests/test_caption_conflict.py, tests/test_wake.py, tests/test_wake_expand.py, tests/test_cli.py, tests/test_zoom.py, tests/test_nap.py, docs/architecture/index.md, README.md, memory-bank/systemPatterns.md, and GitHub issue #67 (fetched live).

## Findings

1. **TDD Plan Encoding — PASS.** Units 1 and 2 both order stub-tests -> stub-interface -> write-tests-red -> write-code-green; no executable unit skips to code-first. Unit 3 (docs) and Unit 4 (apply migrate.py to this clone's stores) correctly claim the prose/no-new-behavior exemption — unit 4 runs an already-tested tool against data, it does not add new production logic. No change-detector tests found in the plan.
2. **Convention compliance — PASS.** Proposed names (`_old_stem`, `_shorten_tree`) match existing snake_case/underscore-private conventions (`_four_part_stem`, `_migrate_store`). `_shorten_tree`'s use of `m._replace(child, id=..., tree=...)` matches the real `_replace(obj, **changes)` helper (line 105) and `NapChild.__slots__ = ("id", "sum", "tree")` (line 142) — the recursive rebuild is structurally valid.
3. **Dependency impact — PASS.** Verified `short_id`, `resolve_id`, `_index_tree`, `named_ids` are width-agnostic (no hardcoded 64 assumptions) — truncation living only inside `leafset_id` is sufficient, confirming the plan's stated decision. Verified `_index_tree` line 818 recomputes note ids fresh via `leafset_id(...)`, so note view-id width follows the same truncation automatically without a separate touchpoint.
4. **Conflict detection — PASS.** No duplicate/overlapping utility found. Confirmed live: root store (`.summem/naps/`) trees nest 64-hex `NapChild.id` at up to depth 4 (grain-32: depth 4, 30 nested ids; grain-16: depth 3; grain-8: depth 2) — this validates the plan's activeContext.md correction of the prior preflight's "no nested id" claim, and confirms recursion is load-bearing for this clone's own unit-4 migration, independent of the unit-2 grain-8 test fixture.
5. **Completeness — PASS.** Every projectbrief.md acceptance criterion (1-7) maps to a concrete unit/step. Verified all cited doc line numbers exist as claimed: `docs/architecture/index.md:97` ("Stored names keep the full id."), `:309` ("64-hex stays on disk."), `:367` (change-surfaces migrate row); `systemPatterns.md` "Filenames and `.tree` identity stay 64 hex" (single occurrence); `README.md:91-92` (64-hex leafset in both pair basenames) and `:106-121` (note-only example `.tree`, correctly left untouched by the plan). No other "64 hex"/"64-hex" prose occurrences exist in canonical docs beyond what units 3 already targets. Fetched issue #67 live: the table, acceptance list, and "not this issue" list match projectbrief.md and tasks.md verbatim in substance.
6. **Test-fixture enumeration — PASS.** Grepped all of `tests/` for `== 64`, `!= 64`, and `* 64` independent of the plan's own list. Every hit is already accounted for: the six leaf-set-id fixtures the plan lists (`test_codec.py` x4, `test_wake.py:58`, `test_wake.py:270`, `test_caption_conflict.py:83`, `test_wake_expand.py:182`) need `16`; `test_zoom.py:64`, `test_nap.py:249` (`"0" * 64` passed as an unknown-id CLI argument) and `test_cli.py:37,46` (`"a"*64` as an argparse-arity placeholder, never parsed as a leafset) are correctly exempted as non-stored sentinels per the plan's own Test Infrastructure note. No missed fixture.
7. **Previous FAIL findings — confirmed resolved.** Second preflight's two defects are both fixed in the current plan text: (a) unit 2 step 1 now specifies a grain-8 two-nested-depth fixture, which a one-level `tree.kids` walk cannot satisfy, correctly pinning `_shorten_tree` recursion; (b) unit 2 step 4 now explicitly forbids `Path.replace` of the source `.tree` onto the destination stem and requires `_write_pair` (verified this helper exists at `summem:410` with the exact signature the plan calls) followed by source unlink.

## Advisory items

1. **Radical innovation (advisory, not applied).** This is the second width-narrowing migration of the same on-disk field (#61: four-part→five-part-64; #67: five-part-64→five-part-16), and `migrate.py` hardcodes each generation's parser/rewriter pair directly into `_migrate_store`. A structurally accretive change: replace the ad hoc `_old_stem` + inline rewrite with an ordered table `_LEGACY_STEM_UPGRADES: list[tuple[parse_fn, upgrade_fn]]`, oldest-to-newest, where `_migrate_store` tries each `parse_fn` against a stem and, on the first match, calls the paired `upgrade_fn` to produce fresh `(tree_bytes, caption_bytes)` for `nap_stem`. Today's two generations (four-part-64, five-part-64) become two table entries instead of one bespoke function; the *next* identity-width or encoding change (the issue explicitly reserves Base32 as "not this issue," implying it may come later) becomes "append one entry," not "rewrite `_old_stem` and `_migrate_store` again." Not adopted for #67 — the two-generation dispatch this issue needs is small enough that the table's indirection isn't yet earning its keep, but it's worth a dedicated issue if a third generation is ever proposed.
2. **Minor docstring staleness (advisory, non-blocking).** `tests/test_fold.py:358` ("emit 8-hex prefixes, not 64-hex ids") and `tests/test_gitutil.py:40` ("not 64-hex ids") are prose docstrings on tests whose assertions are width-agnostic (they derive expected prefixes dynamically via `short_id`); both remain literally true after this change (the full id is no longer 64 hex either, so "not 64-hex ids" still holds), so this is not a defect and not in scope for TDD Plan Encoding. An operator could reword for precision in a later pass but the plan is not required to touch these.
