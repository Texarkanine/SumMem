FAIL (fixable)

1. **TDD Plan Encoding / Completeness**: Implementation Plan step 2 changes `usage_text()` from `.summem/summem` to `summem`, but its test step only names exact assertions in fold and scope output. The existing `tests/test_cli.py` assertion (`"summem wake" in catalog`) already passes with `.summem/summem wake`, so this production change has no planned red test. Tighten an existing catalog assertion or add a test that rejects the old prefix before changing `usage_text()`.
2. **Completeness**: The behavior list requires CLI `zoom` to return rc 1 without a traceback when a nap tree raises either `OSError` or `JSONDecodeError`. Step 4 only plans a direct `zoom_text()` malformed-JSON test. Add CLI-boundary coverage for both parse failure and read failure, including rc 1 and sanitized stderr.
3. **Resolved prior findings**: The revised plan now accounts for all hardcoded printed invocations, their fold/scope tests, `VISION.md`, and the persistent `systemPatterns.md` / `techContext.md` briefing. Proposed locations and implementation touchpoints otherwise match the current codebase.
4. **Advisory — radical innovation**: Centralize the stable displayed executable name in one module constant (for example, `CLI_NAME = "summem"`) and compose `usage_text`, catalog pull lines, and fold prompts from it. This preserves the deliberate stable interface while preventing the three printers from drifting again; it is optional and should be weighed against the small added indirection.
