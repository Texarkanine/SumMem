PASS WITH ADVISORY

TDD Plan Encoding: PASS. Units 1–4 each order stub tests, stub interface, write-tests-red, then write-code-green. Unit 5 is prose/policy with no tests. No change-detector steps to strike; no test/code order swap.

Convention Compliance: PASS. Walkers stay in `tests/gitutil.py`; new `tests/test_gitutil.py` matches `tests/` + pytest. Product changes stay in repo-root `summem` and existing zoom/recall/init tests. Reuses `format_wake_line`, `_projected_child`, `short_id`, `named_ids`, `dated_leaf`. Prompt lockstep stays the existing pair of tests. `SourceFileLoader` on `SCRIPT` matches how `load_summem` already loads the no-suffix driver (and avoids importing `conftest` from `gitutil`).

Dependency Impact: PASS. The prior FAIL is now unit 1: `reaches` / `zoom_reaches` enqueue `NapChild.id` from `Tree.kids`, keep `sentence in` zoom output as the hit, do not parse grain. Consumers (`test_proof_squash.py`, `test_proof_branches.py`, `test_zipper.py`, `test_surgery.py`) stay scheduled through those helpers. Leftover `{id}  text` / `split("  ")` assertions are named for retarget: `test_zoom_two_note_nap_prints_both_texts`, `test_zoom_loose_note_id_prints_the_note`, `test_zoom_nap_of_naps_prints_two_children_not_leaves`, `test_nap_of_two_naps_nests_napchild_and_unions_digests`, `test_recall_matches_nested_nap_caption`. Other zoom/recall tests only substring-check sentences and stay green. `test_wake_expand.py` `test_zoom_expanded_child_id` and `test_proof_conflict.py` only require non-empty / sentence-in output.

Conflict Detection: PASS. Replacing `_zoom_note_line` / `{id}  text` is the work. No second addressing scheme. Wake/fold printer stays closed. Public CLI flags unchanged. View recall switching from `rx.search(format_wake_line)` to `rx.search(node.caption)` is the stated requirement, not a contract break.

Completeness Precheck: PASS. Requirements 1–8 and ACs 1–8 have files and functions. Walker enqueue is unit 1 before zoom code. Nested zoom of a note id is `test_zoom_nested_note_id_prints_dated_leaf`. Prompt substring + lockstep copy is unit 4. Atlas/briefing is unit 5.

Radical Innovation (advisory): Do not leave a third listing printer. `_zoom_kids` and `_recall_nested` should be a loop of `_projected_child` plus `format_wake_line` only — no new f-string line shapes. That is how wake moved and zoom/recall did not.

Advisory: Wake still short_ids against view ids only. Recall/zoom will use `named_ids`. A view pack line from recall can be a longer prefix than wake printed for the same row. That is the plan's zoomability choice; do not reopen the wake printer to match unless the operator asks.

Advisory: Unit 1's named red test is `reaches` under a `zoom_text` monkeypatch. `zoom_reaches` is specified in the same write-code step and will be hit by proofs after unit 2. Change both helpers in that unit so `split()[0]` does not survive on the CLI walker.
