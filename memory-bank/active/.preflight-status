FAIL (fixable)

1. **Dependency Impact / Completeness**: The plan updates the test `SCRIPT` variables to point to the repo-root `summem` driver, but it fails to update the hardcoded `.summem/summem` invocation strings inside the `summem` script itself (e.g., `usage_text`, `format_wake_line` catalog lines, and `fold_request` prompt), as well as the `.summem/summem` references in `VISION.md`.
2. **Dependency Impact**: The plan misses updating the tests that assert on these hardcoded driver paths (`tests/test_fold.py`, `tests/test_scopes.py`, and `tests/test_proof_scopes.py`).
3. **Convention Compliance**: The plan implements the new convention of a repo-root `summem` driver but lacks a step to update `memory-bank/systemPatterns.md` and `memory-bank/techContext.md`, which still state the driver is at `.summem/summem`.

**Advisory item** (Radical Innovation):
Instead of hardcoding `summem` or `.summem/summem` in the `summem` script's output, use `os.path.basename(sys.argv[0])` dynamically. This guarantees accurate usage text and prompts regardless of how the operator or tests invoke the script (e.g., `./summem`, `summem`, or `.summem/summem` for dogfooding).
