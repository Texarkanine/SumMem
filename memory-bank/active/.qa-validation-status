PASS

## Findings

- **KISS / DRY / YAGNI:** PASS. `PROMPT_DOC` is the only new interface, and the implementation reuses `prompt_text()` rather than introducing another prompt generator or write path.
- **Completeness:** PASS. The shipped prompt is lockstep-tested, `init` still prints the prompt and writes nothing, `AGENTS.md` starts with the prompt, and onboarding documentation now uses the shipped file.
- **Regression / Integrity:** PASS. Existing CLI and activation patterns remain intact; no placeholders, TODOs, debug artifacts, or hardcoded behavioral shortcuts were introduced.
- **Documentation:** PASS. README, architecture, system patterns, and tech context consistently describe the shipped prompt and retained `init` behavior.
- **Verification:** PASS. `uvx --with tox tox` passed all 224 tests on Python 3.11, 3.12, 3.13, and 3.14.

## Advisory

- `init` stdout still includes recipe text before `prompt_text()`, so redirecting the entire command is not an exact prompt-only artifact. This was explicitly accepted during preflight and does not block the primary `docs/agents-prompt.md` onboarding path.
