PASS

QA (round 2) of `entry-gate-split` against `tasks.md`, `projectbrief.md`, `creative-entry-gate-split.md`, and `systemPatterns.md`. Reviewed the reworked `summem` (`prompt_text`, `how_to_text`, `init_text`), `tests/test_init.py`, `AGENTS.md`, and the briefing files (`README.md`, `docs/architecture/index.md`, `memory-bank/productContext.md`, `memory-bank/systemPatterns.md`, `memory-bank/techContext.md`).

All 8 test-plan behaviors are present and passing; all 6 Project Brief requirements and all 5 acceptance criteria map to concrete assertions. Round 1's blocking gap (missing `nap` argv in root-wake Usage) is closed: `how_to_text()` now teaches `{AGENT_BIN} nap ID-A ID-B CAPTION`, pinned by `test_how_to_text_is_the_usage_section`. The new `test_prompt_and_how_to_are_disjoint` fences the split so write-rule phrases (genre list, denylist, skip-if-nothing-qualifies) stay out of `how_to_text()` and mechanical phrases (writer-only, `wake --path`, pack grammar) stay out of `prompt_text()` except the one wake handoff line.

All five preflight advisories were applied in-step: the `how_to_text()` "git" forbid is now a named pin, the prefix's intro `{AGENT_BIN}` clause was dropped, `docs/architecture/index.md` gained the sovereignty sentence on the activation definition, `productContext.md` gained the operator-edits-the-write-rule use case, and `systemPatterns.md`'s skip-rule paraphrase no longer names the Usage token.

Requirement 6 (shipped default write rule unchanged) holds: the genre list/denylist/skip text moved verbatim from `how_to_text()` into `prompt_text()`, with no duplicate copy left anywhere else in `summem`. No KISS/DRY/YAGNI violations, no stubs or debris, no missed documentation. `tox -e py311`: 371 passed.

No finding blocks acceptance. Proceed to `/niko-reflect`.
