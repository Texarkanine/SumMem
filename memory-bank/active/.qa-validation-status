PASS

KISS: ACK is two stdout writes on the note branch; no new helper.
DRY: fold_request / nap unchanged; ACK not copied into fold_request.
YAGNI: no configurable marker, no phrase-lock tests.
Completeness: write-then-ACK-then-fold; named retargets; prompt lockstep + README.
Regression: nap stdout contract held; require_entry still rejects before lock.
Integrity: marker is Saved.; no notes/ path, no content-id prefix, no stubs.
Documentation: prompt_text / docs/agents-prompt.md / AGENTS.md / README updated.

Advisories (non-blocking):
- ACK remains after with_store_lock (committed plan), so a heal exception after write still looks like a failed note.
- Atlas fold section does not mention the Saved. prefix; README and the baked prompt do.
