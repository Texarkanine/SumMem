FAIL (build rework)

Semantic review of the `heal-same-text` build (commits a6ffe93..HEAD) against `memory-bank/active/projectbrief.md`, `memory-bank/active/tasks.md`, and `memory-bank/active/creative/creative-leaf-identity.md`. Suite re-run for context: `tox -e py311` → 371 passed, 1 skipped (matches the build's claim).

Production diff is four lines and is clean on KISS, DRY, and YAGNI: the note/note skip is deleted from `_first_overlap` (summem:615-630) and the `left.kind == "nap" or right.kind == "nap"` conjunct is deleted from `write_nap` (summem:716). No new abstraction, no dead parameter, no debug artifact, no TODO. All four acceptance criteria have a pinning test. `tests/gitutil.py::assert_unique_cover` no longer skips note/note and is exercised post-heal by two tests, so the regression detector actually covers the fixed class. Heal termination re-checked for the new equal-grain-1 case: `small_set <= other_set` unlinks the older filename, re-lists, and the next pass finds no overlap.

BLOCKING 1 — Integrity/Regression: three retargeted tests now assert the opposite of what their names claim.
- `tests/test_nap.py::test_write_nap_identical_text_notes_still_concat` asserts `ValueError("overlapping packs")` and that no pack is written. Nothing concats.
- `tests/test_cli.py::test_nap_accepts_prefix_of_identical_notes` asserts exit 1 and `not adjacent`. Nothing is accepted.
- `tests/test_zipper.py::test_identical_notes_nappable_after_heal_view` asserts the pair collapses to one node, i.e. precisely not nappable.
Docstrings were updated correctly in all three; the names were not. A test name is the primary index into this suite (it is what `-k`, failure output, and a reader grepping for behavior see), so three names stating the removed contract will mislead the next contributor into thinking the fix regressed. Fix is a rename each, no logic change.

BLOCKING 2 — Regression: `tests/test_zipper.py::test_two_identical_notes_stay` (line 269) still carries the pre-fix claim. Its docstring reads "Two notes with the same text are not unlinked by leaf-set helpers." `_first_overlap` is a leaf-set helper and now does unlink one of them; only this test's narrow body (`leaf_digests` alone, no `heal_view`) keeps it green. The plan's "leave it alone" correctly protected the body, not a docstring that is now false about the system. Scope the name and docstring to what it actually pins: `leaf_digests` reports the shared digest for both nodes.

BLOCKING 3 — Documentation: `docs/theory.md:241` is broken prose. "Heal and that refuse make a duplicate list a path the view does not offer." — "that refuse" is not a noun phrase, and the sentence is the paragraph's payload. Compounding it, the rewrite dropped the `leafset_id` (list) / `leaf_digests` (set) references, so "A hash of a list would count the same sentence twice" no longer names anything a reader can follow; that list-vs-set asymmetry is still true of the code and is the reason the refuse is needed. Unit 4 was prose with no tests, so QA is its only gate. Rewrite the sentence and re-anchor it to the two functions.

ADVISORY (not blocking):
- `write_nap` still raises `overlapping packs` when both sides are loose notes, which are not packs. The plan permitted keeping one error family and tests pin the string, so this is a deliberate, defensible choice — and it is unreachable from the CLI, since `note` and `nap` both heal first (summem:1363, summem:1416), leaving one view node and the `not adjacent` error instead. Only direct library callers can see the wording. Worth revisiting whenever the overlap error family is next touched.
- `tests/test_zoom.py::test_zoom_keeps_duplicate_note_dates` and `tests/test_recall.py::test_recall_keeps_duplicate_note_dates` now plant a children file via `rematerialize_child` because `write_nap` can no longer build that pack. The coverage is legitimate (there is no migrate, so packs written before this change persist in real stores), but neither test says so; one line of comment would stop a future reader deleting them as unreachable.
- Checked and clean: `docs/architecture/index.md:161` already stated the general rule ("`nap` of two nodes whose leaf sets overlap is rejected"), so the Identity and Zipper edits remove a contradiction rather than adding one. `docs/surgery.md:25,41` still say identical-text notes are distinct files needing disambiguation — accurate, since surgery operates on the on-disk state before any heal, which is exactly the mess it exists for.
