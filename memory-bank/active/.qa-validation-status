PASS
Task: prompt-commit-notes (Level 2)
Date: 2026-08-19
Scope reviewed: summem (prompt_text), AGENTS.md, tests/test_init.py, memory-bank/techContext.md, memory-bank/productContext.md, docs/architecture/index.md

Prior QA blocker resolved: test_prompt_text_teaches_git_publish now asserts "commit them" and "own commit". Both substrings occur only in the publish sentence, so removing that sentence turns the test red. "git add" likewise appears only there.

Acceptance criteria: all four met.
- prompt_text() and the lockstep AGENTS.md block teach `git add` plus commit
- writer-only rule retained (invent filenames / rewrite note bytes / delete by hand)
- techContext.md no longer claims this repo ignores store data; .gitignore confirms it does not
- lockstep and invariant tests hold; new publish test added

Tests: 208 passed (uv run --python 3.11 --with pytest pytest).

Advisories (non-blocking):
- `assert "rewrite" in lower` is a weak token; the adjacent "invent filenames" and "the only writer" asserts carry the writer-only contract.
- README's "Never edit store files by hand" line says nothing about publishing. Out of scope; README defers to AGENTS.md.
