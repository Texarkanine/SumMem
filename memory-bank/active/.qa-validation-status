PASS

Task: summ-caption-suffix (Level 2). Run: QA, 2026-08-25.

Verdict: implementation is acceptable as-is.

The three `summem` sites write and read `.summ`. `NapChild.sum` / `dumps_tree` / `ViewNode.sum_path` are unchanged. Tests in the eight listed files were retargeted; `test_view_ignores_leftover_sum_caption` plants a leftover `.sum` and expects an empty caption. The four committed captions were `git mv`'d to `.summ`. README example path and bullet, and the architecture Naps pair bullets, name `.summ` / `.tree`. The find recipe is not in the tree.

Advisories (do not block):
- Three hardcoded `.summ` tokens remain (preflight said do not add `CAPTION_SUFFIX` this build).
- `_unlink_node` will not delete a leftover `{stem}.sum`; read-side ignore is tested.
- The verified find command is not copied into memory-bank (plan forbade an in-repo copy). The PR body still owes it; progress records prune + `case`, skip-if-dest-exists, nested / checksum / driver / spaces / no-clobber.
- Build also committed a script-written root note and a new x2 nap of two older notes. That is store use, not a suffix-path defect.
