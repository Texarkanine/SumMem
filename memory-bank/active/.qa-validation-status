PASS

Implementation matches the five-unit plan. All acceptance criteria are covered by tests; `uvx --with tox tox` reports 275 passed on py311–py314.

Findings (non-blocking):

KISS/DRY: `tests/test_gitutil.py` duplicates `_find_nap` / `_tree_for` tree lookup that `tests/gitutil.py` already implements as `_find_nap_child` / `_tree_for_id`. Acceptable for the monkeypatch test; could share helpers later.

Completeness (advisory): No dedicated `zoom_reaches` wake-grammar unit test; proof suites (`test_proof_squash`, `test_proof_branches`, `test_zipper`, `test_surgery`) exercise the CLI walker and pass.

Documentation (advisory): `prompt_text()` Other commands still says `x<N> <hash>:` while stdout now prefers `short_id` prefixes. Out of this task's scope; not misleading enough to block.

Known product choice (advisory): Recall/zoom prefix against `named_ids` can be longer than wake's view-id prefix for the same pack — documented in plan and briefing.
