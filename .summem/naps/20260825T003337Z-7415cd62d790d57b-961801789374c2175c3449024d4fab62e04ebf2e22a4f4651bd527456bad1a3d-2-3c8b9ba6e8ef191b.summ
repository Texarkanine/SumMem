Skip a root wake only when a == SumMem Usage == block is still readable. A consumer upgrade is copying the script; init prints the bootstrap and writes nothing; replace an old fat AGENTS.md prefix once.
