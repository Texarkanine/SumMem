Four-part nap stem is a prefix of its five-part twin so lex heal needs no format branch; driver lists five-part stems only and migrate.py is the only four-part reader; child_nap_stem names rematerialize and surgery; started_stores lists git root only when .summem is a directory.
