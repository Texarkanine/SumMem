Catalog is one git ls-files of .summem/config.toml; recall/zoom unique-prefix once and parse each view .tree once (#50). Heal overlap is raw JSON; rematerialize and write_nap still build Tree (#51/#56).
