Nap-variant-stems: four-part stem is a prefix of its five-part twin so lex heal needs no format branch; child_nap_stem names rematerialize and surgery; started_stores lists git root only when .summem is a directory.
