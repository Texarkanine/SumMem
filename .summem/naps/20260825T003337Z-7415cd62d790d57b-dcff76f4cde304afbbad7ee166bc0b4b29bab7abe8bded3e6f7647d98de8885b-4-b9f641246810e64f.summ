Skip a root wake only when a == SumMem Usage == block is still readable. Onboarding is init: prints the bootstrap (prompt_text()), writes nothing; replace an old fat AGENTS.md prefix once. A consumer upgrade is copying the script. Root-wake catalog tests slice that section.
