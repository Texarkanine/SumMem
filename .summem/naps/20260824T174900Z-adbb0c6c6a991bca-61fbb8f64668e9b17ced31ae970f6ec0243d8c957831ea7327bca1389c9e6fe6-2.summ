equal_grain_pair is a fold-test oracle only; fold_request walks adjacent same-grain ViewNodes itself; sys.version_info is immediately after import sys so 3.10 prints the floor message instead of ModuleNotFoundError.
