Wake/recall/zoom share format_wake_line (dated leaves, prefixed packs); recall matches text and captions; walkers enqueue NapChild ids from the tree; fold_request adds --path REL when walk-up would miss; 3.11 floor is right after import sys; named_ids catches _TREE_PARSE_ERRORS.
